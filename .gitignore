node_modules/
# Projeto usa pnpm exclusivamente (pnpm-lock.yaml é o lockfile de verdade) —
# um package-lock.json aqui significa que alguém rodou `npm install` por
# engano; ignorado pra não bloquear o deploy.sh na checagem de git sujo.
package-lock.json
dist/
.next/
.turbo/
coverage/
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
