# PENDENCIAS.md — ConectaObra 2.0
> Quadro vivo de pendências. **Atualizar a cada sessão de trabalho** (humano ou Claude).
> Formato: mover itens entre seções; nunca apagar histórico — usar ~~riscado~~ + data.
> Última atualização: 2026-08-04 · Branch: `main`

---

## 🔴 BLOQUEADORES (impedem progresso — resolver primeiro)

| ID | Pendência | Tarefa afetada | Responsável | Desde |
|---|---|---|---|---|
| P-001 | **Validar stack tecnológico final** — visão citava "decisões fechadas" mas a lista não foi anexada. Confirmar proposta do doc 02 ou substituir. Registrar em `docs/adr/ADR-0001-stack.md` | Todas | Fundador/Tech Lead | 2026-07-24 |
| P-002 | **Escolher PSP/BaaS do escrow** (Asaas / Pagar.me / Iugu / Dock / QI Tech) + abrir conta sandbox | S0-07, todo épico E4 | Tech Lead | 2026-07-24 |
| P-003 | Confirmar **cidade-piloto** (sugerida: Vitória/ES) | Matching E3-03, GTM E10-02 | Fundador | 2026-07-24 |

## 🟡 PENDÊNCIAS ATIVAS (Sprint 0)

| ID | Pendência | Tarefa | Status | Observações |
|---|---|---|---|---|
| P-004 | ~~Push da branch `sprint-0` para o GitHub e abertura do PR~~ | S0-01 | Obsoleto | O branch `sprint-0` nunca existiu — scaffold foi direto pra `main`, e toda a pilha S0-04→E3-02 já foi mesclada em `main` em 2026-07-28. Ver nota no rodapé deste arquivo |
| P-005 | ~~Decidir destino da pasta legada `app/` do commit inicial (migrar conteúdo ou remover)~~ | S0-01 | Resolvido (2026-07-29) | Inspecionado: `app/web/readme.md` e `app/api/readme.md` estavam vazios (0 bytes) — não havia conteúdo real pra migrar. Pasta removida; referência em `CLAUDE.md` §4 também removida |
| P-006 | Escolher provedor de assinatura eletrônica (Clicksign/ZapSign/D4Sign) e de KYC (idwall/CAF) | S0-08 | Aberto | Pode rodar em paralelo ao P-002 |
| P-007 | Rodar `pnpm install` e validar `docker compose up` no ambiente local do time | S0-02 | Parcial | `pnpm install` já roda limpo (lockfile gerado e commitado); `docker compose up` **ainda não validado** — Docker não estava disponível no ambiente desta sessão |
| P-008 | Configurar secrets do GitHub Actions (deploy staging) | S0-03 | Aberto | Workflow de CI criado; job de deploy comentado até definir infra |
| P-009 | Design System v0: 15 componentes base implementados em `packages/ui` — **mesclado em `main`** | S0-04 | Mesclado | Faltam `Table`, `Tooltip`, `RadioGroup`, `Switch`, `Spinner`/`Skeleton`, Storybook e revisão do Product Designer |
| P-010 | Schema Prisma inicial + migração + seed em `services/api` — **mesclado em `main`** | S0-05 | Mesclado | `prisma validate`/`generate`/`tsc --noEmit` passaram; migração **nunca foi aplicada** a um Postgres real (sem Docker em nenhuma sessão até agora) — validar com `docker compose up` + `pnpm --filter @conectaobra/api prisma:deploy` assim que possível |
| P-011 | Sentry DSN + logger estruturado + tabela `audit_log` — **mesclado em `main`** | S0-06 | Mesclado | Bootstrap NestJS mínimo criado (`main.ts`/`app.module.ts`) só para ter algo real a observar; logger (`nestjs-pino`, redact LGPD) + `SentryExceptionFilter` + `AuditLogService` (sanitiza payload) implementados. **Falta o DSN real do Sentry** (`Sentry.init()` é no-op sem ele) e validar contra Postgres real |
| P-012 | Contratar engenheiro civil consultor (pré-requisito do épico E5 — base da IA) | E5-02 | Aberto | Também apoia mediação de disputas |
| P-013 | ~~Imagem `pgvector/pgvector:pg16` do `docker-compose.local.yml` provavelmente não tem a extensão PostGIS~~ | S0-02 | Resolvido (2026-07-29) | Confirmado por pesquisa (Dockerfile oficial do projeto pgvector no GitHub): a imagem é só `FROM postgres:16` + pgvector compilado, sem PostGIS — `CREATE EXTENSION postgis` quebraria mesmo. Corrigido com `infra/docker/Dockerfile.postgres` própria, partindo da imagem oficial `postgis/postgis:16-3.5` e compilando pgvector v0.8.5 por cima, seguindo exatamente a receita do Dockerfile oficial do pgvector. `docker-compose.local.yml` agora builda essa imagem em vez de baixar `pgvector/pgvector:pg16`. **Não testado com `docker build`/`docker compose up` de verdade** (sem Docker disponível nesta sessão, mesma limitação de sempre) — validar assim que houver Docker à mão |
| P-014 | Cadastro/login (e-mail+senha, OTP telefone) + refresh token — **mesclado em `main`** | E1-01 | Mesclado | `POST /auth/{register,login,refresh,logout,otp/request,otp/verify}` implementados com refresh token rotativo (hash em DB, detecta reuso) e OTP com limite de tentativas. **Envio real de SMS não implementado** (stub que só loga o código — depende de P-006). KYC via provedor (E1-03) e rate-limit distribuído (hoje é por instância, não Redis) ficam para tasks seguintes. Toda a árvore de DI foi validada rodando o app de verdade — mas nunca contra um Postgres real |
| P-015 | Onboarding por perfil (cliente PF/PJ, prestador, fornecedor, técnico) — **mesclado em `main`** | E1-02 | Mesclado | `GET /profile/me`, `PUT /profile/prestador` (TECNICO reaproveita o mesmo endpoint/model — doc 02 §3 não define profile próprio pra técnico), `PUT /profile/fornecedor`, com `UserTypeGuard` restringindo cada rota. `geo` gravado via `$executeRaw` (PostGIS). Cliente PF/PJ não tem perfil extra — o cadastro já é o onboarding dele. Validado com `tsc`/`nest build`/execução real; nunca contra um Postgres real |
| P-017 | MFA (TOTP) — setup/enable/disable + desafio no login — **mesclado em `main`** | E1-04 | Mesclado | `POST /auth/mfa/{setup,enable,disable,verify-login}` com `otplib`. Login com MFA ligado devolve `{mfaRequired:true, mfaToken}` em vez dos tokens; `JwtStrategy` rejeita tokens com `scope` != `access`. Isso é só o alicerce: **nenhum endpoint financeiro existe ainda pra de fato "exigir" MFA** (isso vem no épico E4). `otplib` testado isoladamente (generateSecret/keyuri/check); app testado de ponta a ponta (DI limpo); nunca contra um Postgres real |
| P-018 | Upload de mídia (S3 presigned) — **mesclado em `main`** | E1-07 | Mesclado | `POST /media/presigned-upload` gera URL de PUT direto pro S3 (`@aws-sdk/client-s3`). **Nenhum bucket/credencial real existe ainda** — sem isso, o endpoint responde 503 (testado: app sobe normalmente mesmo sem S3 configurado). **Compressão de fotos não implementada**: como o upload é direto client→S3, precisa de um worker assíncrono (BullMQ, já no `docker-compose.local.yml`, mas sem consumer criado) rodando depois do upload — ficou de fora do escopo desta task |
| P-019 | LGPD: consentimentos + exclusão de conta — **mesclado em `main`** | E1-08 | Mesclado | `GET /legal/versions`, `POST /legal/consent` (append-only em `consents`), `DELETE /account` (anonimização, não hard-delete). `POST /auth/register` agora exige `aceitouTermos`/`aceitouPolitica`. **Nenhum texto real de Termos de Uso ou Política de Privacidade existe ainda** (`docs/legal/` vazio) — usa uma versão única (`v0-mvp`). `register()`/`deleteAccount()` corrigidos para rodar em `$transaction` (code review desta sessão). Validado com `tsc`/`nest build`/execução real; nunca contra um Postgres real |
| P-020 | Access tokens (JWT) não são revogados na exclusão de conta nem ao desligar MFA — um token emitido pouco antes continua válido até expirar (`JWT_ACCESS_TTL_SECONDS`, 15min por padrão) | E1-04, E1-08 | Aberto | Trade-off aceitável de JWT stateless com TTL curto — não é um bug a corrigir agora, mas registrar como limitação conhecida (achado em code review). Se algum dia precisar de revogação imediata de access token, a opção padrão é uma denylist em Redis (já no stack) checada no `JwtStrategy.validate()` |
| P-021 | Criar obra (CRUD básico) — **mesclado em `main`** | E3-01 | Mesclado | `POST/GET/GET:id/PATCH /works`, restrito a `CLIENTE_PF`/`CLIENTE_PJ` para criar/editar; leitura restrita ao dono (404 se for de outro cliente). `status` fica livre (`"planejamento"` no create) — workflow de estados fica pra E6. **Geocoding automático (endereço → lat/lng) não implementado** — nenhum provedor escolhido (Google Geocoding/Mapbox/Nominatim); `geo` é opcional, client manda lat/lng direto se tiver. Validado com `tsc`/`nest build`/execução real; nunca contra um Postgres real |
| P-022 | Publicar RFQ — **mesclado em `main`** | E3-02 | Mesclado | `POST/GET/GET:id/PATCH /rfq`, restrito a `CLIENTE_PF`/`CLIENTE_PJ`; exige que a `obraId` pertença ao próprio cliente (404 senão); `PATCH` só enquanto `status = ABERTO`. `Rfq.fotos` (array de URLs do S3, via E1-07) precisou de nova migração — ausente do doc 02 §3. **Sem matching regional, notificação ou propostas ainda** (E3-03/E3-04/E3-05). Validado com `tsc`/`nest build`/execução real; nunca contra um Postgres real |
| P-023 | ~~`WorksService.create()`/`update()` gravam `geo` via `$executeRaw` numa chamada separada do `prisma.work.create()`, sem transação~~ | E3-01 | Resolvido (2026-07-29) | `WorksService.create()`/`update()` e `ProfileService.upsertPrestador()` (mesmo padrão) agora envolvem a escrita principal + o `$executeRaw` do geo numa única `prisma.$transaction()` |
| P-024 | Motor de matching regional — **mesclado em `main`** | E3-03 | Mesclado | `MatchingService.matchRfq()` casa RFQ com até 10 prestadores por categoria + raio (PostGIS `ST_DWithin`), com rodízio via `ProfilePrestador.ultimoMatchEm` (nova migração, junto com `RfqMatch` pra persistir o resultado). Roda automaticamente e de forma best-effort dentro de `RfqService.create()` — falha no matching não impede a publicação do RFQ. `GET /rfq/discover` pro prestador ver os RFQs casados. **Sem obra.geo, o match cai pra só categoria** (geocoding ainda não existe, P-021). Validado com `tsc`/`nest build`/execução real; nunca contra um Postgres real |
| P-025 | Envio de proposta + "caps por plano" — **mesclado em `main`** | E3-05 | Mesclado | `POST/GET /rfq/:id/proposals`, uma proposta por prestador por RFQ (imposto pelo banco via `@@unique`, não só pela aplicação), só enquanto `ABERTO`; listagem restrita (dono vê todas, prestador só a própria — privacidade de leilão). **"Caps por plano" é um placeholder**: os tiers pagos de verdade (E8-01, 79/149/299/599 etc.) não existem ainda — hoje, sem nenhuma `Subscription`, o prestador tem um teto mensal fixo (`FREE_PLAN_MONTHLY_PROPOSAL_LIMIT`, env, default 5); qualquer `Subscription` isenta do teto, independente do plano real. Validado com `tsc`/`nest build`/execução real; nunca contra um Postgres real |
| P-026 | Aceitar proposta → rascunho de contrato — **mesclado em `main`** | E3-07 | Mesclado | `POST /proposals/:id/accept` (dono do RFQ): marca a proposta `ACEITA`, recusa as demais `ENVIADA` do mesmo RFQ, fecha o RFQ (`CONTRATADO`) e cria `Contract` (`"rascunho"`) + `ContractParty` — tudo numa única `$transaction`, com as checagens de estado repetidas DENTRO dela via `updateMany` condicional (fecha race de aceite duplo concorrente, achado em code review). **Sem PDF nem assinatura eletrônica** (E4-01/E4-02). Validado com `tsc`/`nest build`/execução real; nunca contra um Postgres real |
| P-027 | Notificações de RFQ via fila BullMQ — **mesclado em `main`** | E3-04 | Mesclado | `REDIS_URL` agora obrigatório no env (mesmo padrão de `DATABASE_URL` — Redis já é infra decidida, não decisão de fornecedor em aberto). `MatchingService.matchRfq()` enfileira um job por prestador casado (best-effort). **`NotificationsProcessor` é um stub que só loga** — nenhum provedor de push/e-mail/WhatsApp foi escolhido (mesmo padrão do `OtpNotifier`, E1-01; falta decisão de fornecedor, registrar junto com P-006 quando isso for discutido). Testado que a conexão `ioredis` é preguiçosa e não derruba o boot sem Redis disponível — mas nunca rodado contra um Redis real de verdade (sem Docker) |
| P-028 | ~~`RfqProposalService.enforceMonthlyCap()` faz count-then-insert sem lock~~ | E3-05 | Resolvido (2026-07-29) | `submit()` agora roda `pg_advisory_xact_lock(hashtext(proponenteId))` dentro de uma `$transaction` antes de checar o teto e criar a proposta — serializa submissões concorrentes do MESMO prestador (fecha a race) sem travar outros prestadores enviando ao mesmo tempo. Lock transacional, libera sozinho no commit/rollback |
| P-029 | `apps/web` não renova a sessão sozinho — o access token expira em `JWT_ACCESS_TTL_SECONDS` (15min por padrão) e a UI não faz refresh silencioso nem retry-on-401; o usuário só percebe ao navegar de novo e cair no `/entrar` (via `middleware.ts`) | E1-01 (web) | Aberto | `POST /api/auth/refresh` já existe (`apps/web/app/api/auth/refresh/route.ts`) mas nada o chama ainda automaticamente. Corrigir quando a área logada crescer além do placeholder atual (`/conta`) — hoje o escopo era só provar o loop cadastro→sessão→rota protegida→logout |
| P-030 | Nenhum fluxo de MFA (desafio TOTP no login) implementado em `apps/web` — `POST /api/auth/login` devolve 501 se a conta tiver MFA ligado | E1-01 (web), E1-04 | Aberto | Backend já suporta (`mfaRequired: true` + `POST /auth/mfa/verify-login`, ver P-017); falta a tela de "digite o código" no front. Baixa prioridade agora porque nenhuma conta real tem MFA ligado ainda (feature nova, ninguém habilitou) |
| P-031 | `DEPLOY.md`/`deploy.sh` (guia de VPS + script de atualização) nunca foram rodados contra uma VPS de verdade — só validação estática (`bash -n`, ecosystem.config.cjs carregado com `node -e`, chaves do Nginx balanceadas via script Python). Guia agora documenta dois domínios públicos reais (`conectaon.unifyhub.com.br` pro frontend, `apiconectaon.unifyhub.com.br` pro backend) — `services/api` deixou de ser só interno, mas segue sem CORS configurado (não é necessário hoje, `apps/web` nunca chama a API via browser) | Infra | Aberto | Antes do primeiro deploy real: seguir o guia numa VPS de teste do zero e corrigir o que não bater (nomes de pacote apt de PostGIS/pgvector variam por distro/versão do Postgres, por exemplo; confirmar que o certbot com dois `-d` emite certificado único cobrindo os dois domínios como esperado). Também falta decidir backup automatizado do Postgres (DEPLOY.md §13 é manual) e CI/CD de verdade (P-008). Um bug real já foi achado e corrigido nesta sessão seguinte — ver changelog "dist/main.js" |
| P-032 | ~~Prestador não tem tela própria pra ver os detalhes de um RFQ em que propôs~~ | E3-06 (web) | Resolvido (2026-07-29) | `RfqService.getMine()` (usado por `GET /rfq/:id`) agora permite o cliente dono OU qualquer prestador com `RfqProposal` nesse RFQ — `getOwnedOrThrow` continua estrito ao dono e segue usado só por `update()`. `apps/web/app/(app)/rfq/[id]/page.tsx` já tinha a lógica de exibição condicional (`souDono`) pronta desde E3-06; só a mensagem de fallback (404) foi atualizada pra não dizer mais "só funciona pra quem publicou" |
| P-033 | Coeficientes das calculadoras determinísticas (`services/api/src/modules/ai/calculators.service.ts`) ainda não passaram por revisão de engenheiro civil | E5-04 | Aberto | Valores de referência amplamente publicados em guias de construção civil (fator de conversão de traço ~1,53; densidade do cimento ~1400kg/m³; fator água/cimento ~0,55), não texto de NBR. Resultados batem com faixas comumente citadas (blocos: ~12,5/m² pra bloco 9x19x39 — confere com tabelas usuais; argamassa: ~11L/m² — dentro da faixa 10-13L/m² comumente citada), mas dependem do mesmo engenheiro civil consultor de P-012 pra validação formal antes de qualquer uso além de estimativa grosseira |
| P-034 | ~~`GET /search/*` (E2-01/E2-02) exige login (`JwtAuthGuard`), igual o resto da API~~ | E2-01/E2-02, E2-03 | Resolvido (2026-07-29) | `SearchController` não tem mais `@UseGuards(JwtAuthGuard)` — a própria pendência dizia "revisitar quando E2-03 entrar em pauta", e E2-03 (páginas públicas) já foi mesclado em `main` na sessão anterior. Continua coberto pelo rate-limit global (`ThrottlerGuard`) |
| P-035 | Índice `prestadores` do Meilisearch não inclui geolocalização (`_geo`) — busca por texto/categoria funciona, mas não dá pra combinar busca textual com raio geográfico numa única query | E2-01/E2-02 | Aberto | O matching de RFQ (E3-03) já resolve geo+categoria via PostGIS direto, então não bloqueia o loop principal. Portar `geo` (Postgres `Unsupported("geography")`) pro formato `_geo: {lat, lng}` do Meilisearch exigiria uma leitura via raw SQL na hora de indexar — deixado de fora deste primeiro corte |
| P-036 | ~~`POST /contracts/:id/reviews` permite avaliar qualquer contrato do qual eu seja parte, independente do trabalho ter sido de fato entregue~~ | E2-04, E6 | Resolvido (2026-07-29) | `ReviewsService.create()` agora exige pelo menos 1 `Milestone` com status `APROVADO` no contrato (E6-01, `MilestonesService`) antes de aceitar uma avaliação — exatamente a correção antecipada aqui. `Contract.status` em si continua livre (`String`), mas não é mais isso que decide a elegibilidade da avaliação |
| P-037 | "Selo básico" do E2-05 não foi implementado — só a nota agregada (`notaMedia`) | E2-05 | Aberto | As únicas definições de "selo" nos docs (`docs/prd/01`) são: (1) "Perfil Verificado" ligado a KYC, bloqueado por P-006; (2) Bronze/Prata/Ouro/Platinum do job mensal de ranking, que é explicitamente escopo do E8-04 ("critérios objetivos e auditáveis"), uma task separada. Implementar um selo por conta própria aqui seria inventar um critério de negócio não documentado — não fiz isso |
| P-038 | `apps/web/app/prestadores/[id]` e `/fornecedores/[id]` (E2-03) só são alcançáveis digitando a URL direto — não existe nenhuma página de resultado de busca no frontend linkando pra elas | E2-02 (web), E2-03 | Aberto | O backend de busca (`GET /search/*`, E2-01/E2-02) já existe, mas nunca ganhou UI em `apps/web` — só as páginas de perfil público em si (E2-03) foram construídas. Cria uma lacuna de navegação: as páginas de perfil são reais e indexáveis por SEO, mas hoje só chegam nelas por link direto ou pelo Google depois de indexado |
| P-039 | Cronograma de etapas (E6-01) não tem Gantt visual nem dependências entre etapas — só CRUD + máquina de estados (`PENDENTE→EM_EXECUCAO→ENTREGUE→APROVADO`) | E6-01 | Aberto | O backlog descreve E6-01 como "Gantt simplificado, dependências"; o schema (`Milestone.ordem`) só modela ordem sequencial, sem grafo de dependência. Implementei o que já estava modelado (a máquina de estados que casa exatamente com o loop do CLAUDE.md) e deixei de fora a parte visual/Gantt e o grafo de dependências, que exigiriam schema novo e trabalho de frontend significativo — nenhuma UI em `apps/web` foi construída para milestones ainda |
| P-040 | Diário de obra (E6-03) não tem UI em `apps/web` (só o endpoint `GET /works/:id/diario`), não tem "fotos geolocalizadas" (o backlog cita isso; nenhum evento gravado no `audit_log` tem geolocalização de foto) e só cobre eventos que os serviços já instrumentam (obra/RFQ/proposta/contrato/etapa/avaliação) — não dá pra registrar uma entrada manual no diário | E6-03 | Aberto | Escopo deliberado: reaproveitei o `audit_log` (S0-06) em vez de criar uma tabela de eventos dedicada, adicionando só `obraId` opcional nele. Cobre o essencial ("feed automático de eventos") sem inventar geolocalização de fotos que não existe em nenhum upload do sistema hoje (E1-07 não grava lat/lng do EXIF) |
| P-041 | Painel financeiro (E6-02) mostra "previsto × aprovado" em vez de "previsto × realizado" (como o backlog descreve), e não tem UI em `apps/web` (só `GET /works/:id/financeiro`) | E6-02 | Aberto | Decisão deliberada de nomenclatura: sem o escrow (E4, bloqueado por P-002), nenhum dinheiro de verdade se move ainda — "realizado" sugeriria pagamento de fato, o que seria enganoso (CLAUDE.md §5 regra 1: dinheiro é levado a sério neste projeto). `valorAprovadoCentavos` reflete só o valor das etapas com `Milestone.status = APROVADO`/`PAGO`. Quando o escrow existir, revisitar pra separar "aprovado" (liberação autorizada) de "pago de fato" (webhook do PSP confirmado) |
| P-042 | Gestão de equipe da obra (E6-04) não tem UI em `apps/web` (só `POST/GET/DELETE /works/:id/equipe`), e a visibilidade compartilhada só chegou até obra/diário/painel financeiro/etapas — RFQ, propostas e contratos continuam exclusivos das partes originais (`ContractParty`), não da equipe | E6-04 | Aberto | Escopo confirmado com o usuário via checklist de decisão: **visualização compartilhada, só leitura** — membro da equipe nunca ganha poder de escrita (criar RFQ, aprovar etapa, aceitar proposta continuam exclusivos do cliente dono). Estender a mesma visibilidade a RFQ/propostas/contratos não foi pedido e ampliaria a superfície de dados expostos a terceiros sem uma decisão explícita sobre isso — deixado de fora até haver demanda |
| P-043 | Lista de materiais (E7-01) só cobre criação/edição **manual** — sem "importação da IA" (`origem: IA` existe no schema desde S0-05, mas nunca foi usado). Sem UI em `apps/web` pra nada do épico E7 até agora | E7-01 | Aberto | Import via IA depende do "Engenheiro Virtual" (E5, RAG sobre NBR/SINAPI/CUB), que ainda não existe — implementar aqui seria inventar uma extração de lista de materiais sem a base de IA real por trás |
| P-044 | Cotação automática (E7-02, `POST /material-lists/:id/quote`) só casa fornecedor por `ProfileFornecedor.categorias` — sem geo/raio (diferente do matching de RFQ, E3-03) e sem rodízio (`ProfilePrestador.ultimoMatchEm` não tem equivalente em `ProfileFornecedor`, quem tem `notaMedia` mais alta é sempre priorizado). Item de lista sem `categoria` preenchida nunca entra no matching (campo é texto livre opcional, sem ligação com o catálogo de produtos, E1-06) | E7-02 | Aberto | Escopo mínimo pra ter o loop "lista → cotação → resposta do fornecedor" funcionando; region/raio ficaria estranho pra entrega de material (não é atendimento presencial como RFQ de serviço) — mas pode fazer sentido revisitar quando frete/logística (fora do escopo atual) entrar em pauta. Falta de rodízio pode concentrar cotações nos fornecedores com nota mais alta, prejudicando quem está começando na plataforma — mesmo cuidado que motivou o rodízio de RFQ (E3-03) |
| P-045 | Comparador (E7-03, `GET /material-lists/:id/comparison`) só marca destaques por critério isolado (menor preço/frete/prazo, melhor avaliação) — sem pontuação combinada nem "melhor custo-benefício geral", e sem UI em `apps/web` | E7-03 | Aberto | O backlog (`04_Tasks_Backlog.md`) só descreve "menor preço, menor frete, melhor avaliação, prazo" sem pedir um score único — inventar uma fórmula de peso combinado seria uma decisão de produto não documentada. Cliente vê os 4 destaques lado a lado e decide; um ranking combinado pode ser revisitado se o backlog vier a especificar os pesos |
| P-046 | **Checkout de compra (E7-04, `POST /purchase-quotes/:id/checkout`) usa PSP SIMULADO — nenhum dinheiro real muda de mãos.** `pspRef` é sempre `SIMULADO-<uuid>`, sem chamada a nenhum provedor externo; a "aprovação" é sempre instantânea e sempre bem-sucedida (não existe caminho de falha/recusa/estorno). Comissão (`PROCUREMENT_COMMISSION_BPS`, default 5%) é um placeholder dentro da faixa "3–8%" do backlog, não uma decisão de negócio fechada. Sem NFS-e da comissão, sem extrato/histórico de compras (`GET`) e sem UI em `apps/web` | E7-04, E4 | Aberto (bloqueado por P-002) | Implementado assim a pedido explícito do usuário — "continuar sem o PSP, simulando sucesso" — pra não travar o épico E7 esperando a escolha do parceiro BaaS. `PurchaseOrder` já modela os campos monetários certos (centavos, idempotente via `@unique` em `purchaseQuoteId`) para quando o PSP real (P-002) existir: aí a criação do registro passa a depender de um webhook confirmado, não de sucesso automático. **Não usar este fluxo como base de faturamento real** — é só pra destravar desenvolvimento de UI/fluxo |
| P-047 | **Hub Financeiro/Escrow (E4, núcleo do produto) rodando com PSP SIMULADO — nenhum dinheiro real muda de mãos, nem em produção.** `POST /contracts/:id/milestones/:id/deposito` e a liberação automática (`MilestonesService.aprovar()` → `EscrowService.liberarSeDepositado()`) sempre têm sucesso instantâneo; `pspRef` é sempre `SIMULADO-<uuid>`. Comissão (`ESCROW_COMMISSION_BPS`, default 3%) é placeholder dentro da faixa "2–5%" do PRD, não decisão fechada. **Não implementado**: E4-01 (contrato PDF)/E4-02 (assinatura eletrônica — depende de P-006), E4-11 (webhook — não aplicável a um PSP simulado, só faz sentido quando o PSP real existir), E4-13 (NFS-e). `GET /contracts/:id/escrow` devolve o ledger cru de um contrato; `GET /extrato-financeiro` (E4-12) já cobre a visão por usuário — sem UI em `apps/web` pra nenhum dos dois. Ledger com hash encadeado (SHA-256, `pg_advisory_xact_lock` pra concorrência) validado fora do framework (`node -e`): cadeia íntegra e líquido+comissão sempre bate com o valor depositado | E4 | Aberto (bloqueado por P-002) | Implementado assim a pedido explícito do usuário — "simule como se fosse real... de forma a não impedir o desenvolvimento do restante". Depósito é **opt-in por etapa**: milestones aprovadas sem depósito prévio continuam funcionando exatamente como antes (`APROVADO`, sem tocar no ledger) — decisão confirmada com o usuário via checklist nesta sessão. **Corrigido de quebra**: `ReviewsService.create()` agora aceita `APROVADO` OU `PAGO` no gate de elegibilidade (antes só aceitava `APROVADO` — sem o ajuste, contratos com etapas já liberadas pelo escrow parariam de contar, o oposto do esperado). **E4-09/E4-10 (disputa/estorno), E4-08 (timeout de aprovação) e E4-12 (extrato financeiro) já foram implementados** — ver P-048/P-049/P-050. **Não usar este fluxo como base de operação financeira real** até o P-002 ser resolvido |
| P-048 | Fluxo de disputa (E4-09/E4-10) — `POST .../disputas` (abrir, qualquer parte do contrato), `GET .../disputas` (listar da etapa), `GET /disputas` (fila de mediação, ADMIN) e `PATCH /disputas/:id/resolver` (ADMIN: `APROVAR`/`ESTORNAR`/`LIBERAR_PARCIAL`). Sem SLA/prazo pra mediação, sem notificação quando uma disputa é aberta ou resolvida (E3-04/BullMQ não integrado aqui), e sem UI em `apps/web`/`apps/admin`. `LIBERAR_PARCIAL` exige que o mediador informe o valor exato em centavos manualmente — sem calculadora de proporção (ex.: "40% pro prestador") na API, isso fica pro front | E4-09, E4-10 | Aberto | ~~Nenhum usuário ADMIN existe~~ — `prisma/seed.ts` agora cria um (`admin@conectaobra.example.com`), mas **o seed nunca foi rodado contra um Postgres real nesta sessão** (mesma limitação de sempre, ver P-010) — validar assim que houver banco disponível. Notificação de disputa seguiria o mesmo padrão já usado em `MatchingService` (fila BullMQ best-effort) — deixado de fora pra não expandir mais o escopo desta rodada |
| P-049 | Aprovação automática por timeout (E4-08) — jobs BullMQ (fila `milestone-timeout`) agendados em `entregar()`: avisos em D-3/D-1 (só logam, sem provedor real, mesmo stub de `NotificationsProcessor`) e auto-aprovação em D-0 (`MILESTONE_AUTO_APROVACAO_DIAS`, default 7, confirmado com o usuário). A confiabilidade do timeout depende inteiramente do Redis manter os jobs delayed até vencerem (persistência RDB/AOF configurada e worker sempre rodando) — se o Redis perder o job por qualquer motivo, a etapa fica em `ENTREGUE` indefinidamente até aprovação manual ou disputa, sem nenhum mecanismo de reconciliação lendo direto do Postgres como fallback. Sem endpoint de consulta ("quantos dias faltam pra essa etapa auto-aprovar") — só é visível no log do worker. Não testado contra um Redis/worker real (mesma limitação de sempre, sem Docker nesta sessão) | E4-08 | Aberto | Um job de reconciliação diário (relê etapas `ENTREGUE` há mais de N dias direto do Postgres, fonte de verdade, e completa quem os jobs BullMQ deveriam ter pego) seria a correção mais robusta contra perda de job — deixado de fora pra não expandir mais o escopo desta rodada; revisitar se isso se provar um problema real em produção |
| P-050 | Extrato financeiro (E4-12, `GET /extrato-financeiro`) só mostra transações `LIBERACAO` (o que o prestador/fornecedor recebeu) — não inclui `ESTORNO`/`COMISSAO` no histórico do usuário, não tem paginação (devolve tudo de uma vez) nem filtro por período/contrato, e não gera nenhum comprovante formatado (PDF/recibo) além de expor o `pspRef` sintético como referência. Sem UI em `apps/web` | E4-12 | Aberto | Escopo mínimo que cobre a pergunta central do backlog ("quanto eu recebi e de qual etapa") — paginação/filtros/PDF de comprovante ficam pra quando houver demanda real de uso, evitando construir UI/parâmetros especulativos |
| P-051 | **Pipeline RAG (E5-01) usa embeddings SIMULADOS — sem qualidade semântica real.** `embedText()` é feature hashing determinístico (bag-of-words: só captura sobreposição exata de palavras/radicais, sem sinônimos, sem entender contexto) — funciona pra validar o pipeline de ponta a ponta (ingestão → chunking → storage pgvector → retrieval por cosseno), mas a qualidade de busca de um chat real seria ruim. Sem índice `ivfflat`/`hnsw` na coluna `embedding` (aceitável pro volume pequeno de conteúdo curado hoje; sequential scan funciona, mas não escala). `POST /ai/knowledge` (ingestão) não tem nenhuma validação de que o `conteudo` é de fato um resumo autoral e não texto integral de norma — depende de disciplina de quem ingere (ADMIN). Sem UI de administração de conteúdo | E5-01 | Aberto (embeddings bloqueados por decisão de fornecedor, mesma categoria de P-002/P-006) | Simulação implementada a pedido explícito do usuário — mesmo espírito do PSP simulado (E4), pra não travar o pipeline esperando a escolha de um provedor real de embeddings (OpenAI/Voyage AI/Cohere). Trocar por um provedor real exige só reescrever `embeddings.util.ts`; o resto do pipeline (chunking, schema, retrieval) não muda. Índice vetorial e validação de conteúdo ficam pra quando o volume de dados e o processo de curadoria (E5-02, bloqueado por P-012) justificarem |
| P-052 | **Chat do "Engenheiro Virtual" (E5-03) usa resposta SIMULADA — sem Claude API real, `ANTHROPIC_API_KEY` não configurada.** `AiChatService` não gera texto de verdade: monta uma frase-template citando os títulos/fontes recuperados pelo RAG (E5-01), sem síntese, sem raciocínio, sem responder perguntas fora do que está literalmente na base de conhecimento. Sem streaming real (a resposta chega inteira de uma vez — streaming token-a-token só faz sentido com um LLM real por trás). `tokensUsados` é uma estimativa grosseira (~4 caracteres/token), não uma contagem real de tokens. Conversas são persistidas em `AiConversation` sem checar um consentimento específico de IA (usa só o consentimento geral de termos/privacidade do registro, E1-08) | E5-03 | Aberto (bloqueado por decisão de fornecedor — Claude API real, mesma categoria de P-002/P-006/P-051) | Simulação implementada a pedido explícito do usuário — mesmo espírito do PSP/embeddings simulados, pra não travar o E5-03 esperando a integração real da Claude API. Trocar por uma chamada real exige reescrever só `montarResposta()`/streaming em `AiChatService` — o resto (RAG, persistência, disclaimer) não muda. **Rate-limit, log de auditoria e classificador de risco expandido já foram implementados (E5-06)** — ver P-054 |
| P-053 | **Analisador de orçamento (E5-05) e gerador de lista via IA (E5-07) não têm dado real por trás.** `AvgCost` (E5-05) está vazio — nenhum SINAPI/CUB real foi carregado, só `POST /ai/avg-costs` (ADMIN) manual; sem isso, `POST /ai/analisar-orcamento` sempre devolve "nenhum dado encontrado". `MaterialGeneratorService` (E5-07) é regras por palavra-chave simples (não um LLM): só reconhece pintura/piso/revestimento/alvenaria/elétrica/hidráulica em português, sem sinônimos além dos listados, e itens sem calculadora determinística (elétrica/hidráulica) viram "verba" com quantidade fixa 1, pedindo revisão manual — deliberado, pra não fabricar uma quantidade de projeto executivo do nada (CLAUDE.md §5 regra 3). Sem UI em `apps/web` pra nenhum dos dois | E5-05, E5-07 | Aberto | Carga de dado real de custo médio (SINAPI/CUB) é uma decisão de fonte de dados — mesma categoria de P-012 (curadoria da base, precisa do engenheiro consultor validando os valores). Gerador de lista mais sofisticado (sinônimos, mais categorias, extração de área da própria descrição via NLP) só faz sentido com um LLM real por trás (mesmo bloqueio de P-052) |
| P-054 | Guard-rails do chat de IA (E5-06) — `AI_CHAT_FREE_PLAN_DAILY_LIMIT` (default 10/dia, placeholder até E8-01) conta mensagens de hoje varrendo **todas** as conversas do usuário e filtrando pelo `createdAt` de cada mensagem dentro do Json (`AiConversation` não tem `updatedAt` pra filtrar só linhas tocadas hoje) — funciona, mas não escala bem se um usuário acumular muitas conversas ao longo do tempo. Sem lock de concorrência (duas mensagens simultâneas do mesmo usuário podem passar do limite por 1, race de baixa severidade — mesma categoria que motivou o lock de P-028, mas não replicado aqui por ser bem menos crítico que RFQ). Classificador de risco continua por palavra-chave (lista ampliada: rachadura/trinca/fissura/recalque, curto-circuito/choque elétrico, cheiro de gás) — ainda não é um classificador de intenção real, falsos negativos continuam possíveis. Log de auditoria (`ai_chat.mensagem`) registra que uma mensagem foi enviada e se o disclaimer disparou, mas não o conteúdo da conversa em si (evita duplicar dado sensível já em `AiConversation`) | E5-06 | Aberto | Rate-limit e log de auditoria cobrem o essencial do checklist B.4 sem exigir uma contagem de tokens real ou infraestrutura de fila dedicada. Consentimento de IA dedicado (LGPD) e um classificador de intenção de verdade continuam dependendo de decisões fora do código (jurídico e um LLM real, respectivamente) — ver P-052 |
| P-055 | Lojas/filiais do fornecedor (`POST/GET/PATCH/DELETE /profile/fornecedor/lojas[/:id]`) — sem geocoding automático (endereço → lat/lng, mesma limitação de P-021: `geo` é opcional, o fornecedor manda lat/lng direto se tiver), sem vínculo entre `Product`/`PurchaseQuote` e uma loja específica (o catálogo e as cotações continuam por fornecedor como um todo, não por unidade — se isso for necessário no futuro, precisa de uma FK opcional `lojaId` em `Product`), e sem UI em `apps/web`. Essa é a 1ª de 3 rodadas do pedido do usuário (lojas → promoções → produtos+Excel) | E1-06 (extensão) | Aberto | Escopo mínimo: mesmo padrão de `PortfolioItem`/`WorkTeamMember` (CRUD simples ligado ao dono). Vincular produto/cotação a uma loja específica não foi pedido — fica pra quando houver demanda real (ex.: cliente quer saber de qual loja o produto sai) |
| P-056 | Promoções do fornecedor (`POST/GET/PATCH/DELETE /profile/fornecedor/promocoes[/:id]` privado + `GET /public/promocoes` sem login) — 2ª de 3 rodadas do pedido do usuário (lojas → promoções → produtos+Excel). Sem moderação/aprovação de conteúdo (o fornecedor publica direto, sem revisão do ADMIN antes de aparecer na página pública — risco de conteúdo enganoso/spam sem curadoria), sem contagem de uso do código (não há tabela de resgate — não dá pra saber quantas vezes um código foi usado nem vincular a uma compra/RFQ real, já que não existe checkout ligado a cupom hoje), imagem promocional é só uma URL (`imagemUrl`, aceita qualquer URL válida — o front decide como obter essa URL via `POST /media/presigned-upload`; não há validação de que a imagem existe ou é apropriada), e sem UI em `apps/web` (nem o painel do fornecedor, nem a página pública, nem os destaques da home). Filtro de destaque (`?destaque=true`) existe no endpoint público, mas não há limite de quantas promoções um fornecedor pode marcar como destaque — um fornecedor poderia marcar todas as suas promoções, "empurrando" concorrentes pra fora da primeira leva da home | E1-06 (extensão) | Aberto | Moderação de conteúdo e limite de destaques por fornecedor são decisões de produto (quantas vagas de destaque existem, é pago ou por ordem de chegada?) — não especificadas no pedido original, fica pra quando isso virar demanda real. Contagem de uso do código só faz sentido quando existir um fluxo de checkout que aceite cupom (hoje só `PurchaseQuotesService.checkout()`, E7-04, e não lê nenhum código promocional) |
| P-057 | Produtos com código + import Excel (`POST /products/import`, multipart, campo `file`) — 3ª e última rodada do pedido do usuário (lojas → promoções → produtos+Excel). `Product` ganhou `codigo` (único por fornecedor) e `descricao`, mas a planilha só tem 4 colunas (código, descrição, unidade, valor) — sem coluna de categoria, todo produto importado cai em `categoria = "Geral"` (o fornecedor tem que editar manualmente depois se quiser categorizar pra valer, o que afeta a busca filtrável por categoria no Meilisearch). Import é upsert por (fornecedorId, codigo): reenviar a mesma planilha atualiza os produtos existentes, não duplica — mas também significa que não há como saber, só olhando a resposta, quais produtos foram *removidos* da planilha (o import nunca deleta, só cria/atualiza). Processamento é linha a linha (não é uma transação única): uma falha isolada não derruba o restante do import, mas também significa que um import parcialmente bem-sucedido não é atômico. Parser de planilha (`xlsx-import.util.ts`, biblioteca `xlsx`/SheetJS) só lê a primeira aba e só reconhece um conjunto fixo de sinônimos de cabeçalho (ver `HEADER_ALIASES`) — cabeçalho fora desses sinônimos falha com uma mensagem genérica de "colunas não encontradas". Limite de 5MB por arquivo (`MAX_IMPORT_BYTES`, constante — mesmo padrão de `MediaService.MAX_UPLOAD_BYTES`), sem streaming/processamento assíncrono (planilhas muito grandes bloqueiam a request). Sem UI em `apps/web` (nem o formulário manual com os campos novos, nem o botão de upload de planilha) | E1-06/E2-06 (extensão) | Aberto | Categoria obrigatória na planilha não foi pedida pelo usuário (só código/descrição/unidade/valor) — inventar uma coluna a mais seria escopo não solicitado; "Geral" como default deixa o produto pesquisável e editável sem bloquear o import. Import assíncrono (fila BullMQ, já usada em outros pontos do sistema) só compensa se o volume real de linhas por planilha se provar grande o suficiente pra travar a request síncrona — não medido ainda |
| P-059 | Painel do fornecedor em `apps/web` (`/conta/fornecedor`) — 1ª rodada de telas do frontend (perfil + lojas; promoções/produtos ficam pras próximas rodadas). Campos de lista (categorias, regiões, certificações) usam um `Input` de texto separado por vírgula, sem tag-input dedicado — funcional mas fácil de digitar errado (vírgula a mais gera item vazio, já filtrado, mas sem feedback visual de "isso vai virar 3 tags"). Formulário de loja não expõe `geo` (lat/lng) — o campo é opcional na API mas não tem UI ainda, então toda loja criada por aqui fica sem geolocalização (mesma limitação de P-055, agora também sem caminho manual pela tela). **Não testado contra um backend real** (sem Docker/Postgres neste ambiente, mesma limitação de sempre) — validado só até onde dá: `tsc`/`next build` compilam, o middleware bloqueia `/conta/fornecedor` sem sessão, e a página trata corretamente indisponibilidade da API (testado navegando com um cookie de sessão falso). O fluxo de fato criar/editar/excluir uma loja e salvar o perfil nunca rodou contra a API de verdade | E1-06 (extensão, frontend) | Aberto | Tag-input dedicado é polimento de UX que pode esperar uso real; lat/lng manual na tela de loja só vale a pena se o geocoding automático (P-055) continuar não saindo do papel — por ora, cadastrar sem geo não trava nada (campo opcional). Teste end-to-end fica pendente até haver Docker disponível |
| P-060 | Telas de promoções em `apps/web` — 2ª rodada de telas do frontend: aba Promoções no painel do fornecedor (CRUD completo), página pública `/promocoes` (sem login) e destaques na home. Imagem promocional continua só um campo de texto pra URL (`imagemUrl`) — sem widget de upload, o fornecedor precisa colar uma URL já hospedada em algum lugar (mesma limitação de P-056, agora exposta na UI: nada impede colar uma URL inválida ou de um domínio bloqueado por CSP em produção). `next/image` usado com `unoptimized` (URLs arbitrárias de fornecedor, sem allowlist de domínio) — funciona mas perde a otimização automática de imagem do Next. Destaques da home são decorativos "fail open": se a API cair, a seção simplesmente some da home (sem alerta de erro) — comportamento intencional pra não quebrar a landing page, mas também significa que uma falha ali passa despercebida sem monitoramento. **Não testado contra um backend real** (sem Docker/Postgres, mesma limitação de sempre) — validado até onde dá: `tsc`/`next build` compilam, `/promocoes` e a home tratam indisponibilidade da API corretamente, a aba do painel carrega sem erro de console (com cookie de sessão falso) — o CRUD de promoção e a listagem pública real nunca rodaram contra dados de verdade | E1-06 (extensão, frontend) | Aberto | Upload de imagem (reusar `POST /media/presigned-upload`, já existe desde E1-07) fica pra quando isso incomodar de verdade — colar URL já desbloqueia o cadastro. Teste end-to-end fica pendente até haver Docker disponível |
| P-061 | Aba Produtos no painel do fornecedor (`produtos-panel.tsx`) — 3ª e última rodada de telas do frontend (lojas → promoções → produtos+Excel). Formulário manual não expõe `fotos` (array de URLs) — todo produto criado/editado pela tela fica sem fotos, mesma lacuna de "sem widget de upload" já registrada em P-060 pra imagem de promoção. Após um import de planilha bem-sucedido, a tela recarrega a página inteira (`window.location.reload()`) em vez de re-buscar só a lista — funciona mas perde qualquer estado não salvo nas outras abas (ex.: um formulário de loja/promoção aberto e não salvo é descartado); decisão consciente porque o import faz upsert direto no banco e não devolve a lista completa atualizada, só um resumo (criados/atualizados/erros). Handler de import (`app/api/products/import/route.ts`) não pode usar `apiFetchOrThrow` (ela força `content-type: application/json`, que quebraria o boundary multipart) — chama `fetch` direto pro `services/api`, então não passa pelo tratamento padrão de indisponibilidade usado no resto do app (mensagem de erro genérica via try/catch local, mas sem o texto padronizado de "ApiUnavailableError"). **Não testado contra um backend real** (sem Docker/Postgres, mesma limitação de sempre) — validado até onde dá: `tsc`/`next build` compilam, a aba carrega sem erro de console (cookie de sessão falso). O CRUD de produto e o import de planilha de verdade nunca rodaram contra a API real | E1-06/E2-06 (extensão, frontend) | Aberto | Upload de fotos (mesmo padrão de imagem de promoção, P-060) fica pra quando houver demanda; recarregar a lista inteira via reload é aceitável pro volume esperado de uso (fornecedor confere o resultado do import e seus outros cadastros não são coisas que ele espera continuar "em progresso" durante um import). Teste end-to-end fica pendente até haver Docker disponível |
| P-062 | Painel do prestador em `apps/web` (`/conta/prestador`) — telas de Perfil (categorias/experiência/certificados/raio de atendimento) e Portfólio (CRUD de itens com título/descrição), última peça adiada desde a Round 1 do frontend (o backend de prestador — cadastro, login, perfil, portfólio, propostas de RFQ — já existia integralmente desde antes desta rodada de telas). Mesmas lacunas já registradas nas telas de fornecedor: campos de lista usam `Input` separado por vírgula (sem tag-input), formulário de perfil não expõe `geo` (lat/lng), e o portfólio não expõe `fotos` (array de URLs) — sem widget de upload em nenhum dos dois. **Não testado contra um backend real** (sem Docker/Postgres, mesma limitação de sempre) — validado até onde dá: `tsc`/`next build` compilam, o middleware bloqueia `/conta/prestador` sem sessão (confirmado removendo o cookie e vendo o redirect pra `/entrar?redirect=%2Fconta%2Fprestador` no log do dev server) e a página trata indisponibilidade da API corretamente. CRUD real de perfil/portfólio nunca rodou contra a API de verdade | E1-05/E2-04 (extensão, frontend) | Aberto | Tag-input, lat/lng manual e upload de fotos são o mesmo polimento de UX já adiado nas telas de fornecedor (P-059/P-060/P-061) — mesmo critério: esperar demanda real de uso antes de construir. Teste end-to-end fica pendente até haver Docker disponível |
| P-063 | **Anúncios de fornecedor/prestador (E9-06, escopo bem reduzido) — sem cobrança/billing real, `budgetCentavos` é só um campo informativo.** `POST/GET/PATCH/DELETE /ads` (FORNECEDOR/PRESTADOR/TECNICO) + `GET /public/ads` (sem login, home) usam o model `Ad` que já existia desde S0-05 (só ganhou `ativo Boolean`, pra controlar visibilidade). `tipo` (CPC/CPM/DESTAQUE) é só um rótulo hoje — não muda como o anúncio é cobrado nem como é ordenado/priorizado na home, já que não há leilão/ranking de anúncios, nem contagem de clique/impressão, nem desconto de budget (`metricas` continua `null`, nunca escrito). Sem moderação de conteúdo (qualquer FORNECEDOR/PRESTADOR/TECNICO publica direto, sem revisão do ADMIN). `criativo` (Json) ganhou uma forma validada (`titulo`/`descricao`/`imagemUrl`/`linkUrl`) mas imagem continua sendo só uma URL colada — mesma limitação de imagem de promoção (P-060). Sem UI em `apps/web` ainda — ver próxima rodada | E9-06 (escopo reduzido) | Aberto (cobrança real bloqueada por decisão de produto/billing, mesma categoria de P-002) | Confirmado com o usuário via `AskUserQuestion`: CRUD completo (fornecedor e prestador cadastram os próprios anúncios) mas **sem simular cobrança/métricas** nesta rodada — ao contrário do PSP/embeddings/Claude API (onde simular era necessário pra não travar o desenvolvimento de um fluxo central), cobrança de anúncio não bloqueia nada crítico do produto, então full billing fica pra quando o E9-06 for de fato priorizado |
| P-064 | Telas de anúncios em `apps/web` — aba "Anúncios" compartilhada (`components/ads-panel.tsx`) nos painéis de fornecedor e prestador (CRUD: tipo CPC/CPM/DESTAQUE, título/descrição/imagem/link, budget em R$ informativo, ativo/inativo) + seção "Anúncios de fornecedores e prestadores" na home (`GET /public/ads?limit=3`, fail open — mesmo padrão de destaques de promoção). Card do anúncio na home linka pro `linkUrl` do criativo quando existe (abre em nova aba); sem `linkUrl`, cai no perfil público do anunciante (`/fornecedores/:id` ou `/prestadores/:id`, conforme `anuncianteTipo`) — mas isso significa que um anúncio sem link nenhum configurado ainda assim é clicável, só que leva pro perfil, não pra "nenhum lugar". Mesma lacuna de "imagem só por URL colada" já registrada em P-060/P-061/P-062 — sem widget de upload. **Não testado contra um backend real** (sem Docker/Postgres, mesma limitação de sempre) — validado até onde dá: `tsc`/`next build` compilam, a home e as duas abas carregam sem erro de console (com cookie de sessão falso), tratamento de indisponibilidade da API confirmado. CRUD real de anúncio nunca rodou contra a API de verdade | E9-06 (escopo reduzido, frontend) | Aberto | Upload de imagem fica pro mesmo critério das rodadas anteriores (esperar demanda real). Um "sem link configurado" cair no perfil do anunciante é um fallback razoável (visitante ainda chega em algum lugar relevante) — não fabricar um placeholder "anúncio sem destino" seria pior UX. Teste end-to-end fica pendente até haver Docker disponível |
| P-065 | Notícias + biblioteca (E9-01/E9-04, backend) — `Article` (existia desde S0-05, sem endpoints) ganhou `arquivoUrl` opcional (link de PDF de guia/checklist/modelo de contrato) e agora tem CRUD completo (`POST/GET/PATCH/DELETE /articles`, ADMIN-only) + listagem pública sem login (`GET /public/articles?categoria=&limit=`, `GET /public/articles/:slug`). Notícias e biblioteca **compartilham o mesmo model e os mesmos endpoints** — diferenciados só por convenção de `categoria` (não é um enum, então nada impede um valor de categoria inconsistente/digitado errado tanto pro portal de notícias quanto pra biblioteca). Slug é gerado automaticamente do título (com sufixo -2/-3 em colisão) se o ADMIN não informar um explicitamente; se informar, a unicidade é exigida sem desambiguação silenciosa. Sem paginação de verdade na listagem pública (só `limit`, sem cursor/offset). Sem UI em `apps/web` (nem painel ADMIN pra cadastrar, nem portal público de notícias/biblioteca) — fica pra próxima rodada | E9-01, E9-04 | Aberto | Reaproveitar `Article` em vez de criar um model de biblioteca dedicado evita duplicar campos quase idênticos (título/categoria/corpo/autor) — a diferença real (link de arquivo) já existia como necessidade única, resolvida com um campo opcional. Um enum de categoria foi considerado e descartado: notícias e biblioteca têm categorias de natureza bem diferente (editorial vs. tipo de documento), forçar as duas num único enum seria arbitrário sem uma lista fechada vinda do produto |
| P-066 | Indicadores de mercado + tabela de custos médios (E9-02/E9-03, backend) — `Indicator` e `AvgCost` já existiam desde S0-05 (o segundo já tinha cadastro ADMIN desde E5-05, `POST /ai/avg-costs`; o primeiro não tinha nenhum endpoint). `POST /indicators` (ADMIN, upsert por tipo+região+mês de referência) cadastra manualmente CUB/INCC/SINAPI/aço/cimento/madeira — **confirmado com o usuário via `AskUserQuestion`: sem ingestão automática de fonte externa** (nenhuma API de indicador foi escolhida, mesma categoria de decisão do PSP/P-002). `GET /public/indicators` e `GET /public/avg-costs` (sem login) expõem os dois pra consulta pública — mas como o cadastro é 100% manual, a tabela só terá dados reais quando alguém (ADMIN) inserir; sem seed de indicadores reais, os endpoints públicos hoje devolveriam listas vazias em qualquer ambiente novo. Sem paginação de verdade (só `limit`) em nenhum dos dois. Sem UI em `apps/web` (nem painel ADMIN de cadastro, nem página pública das tabelas) | E9-02, E9-03 | Aberto (ingestão automática bloqueada por decisão de fonte de dados, mesma categoria de P-012/P-053) | Ingestão automática de verdade (scraping/API do IBGE, Sinduscon, CBIC etc.) só faz sentido depois de uma fonte ser escolhida e validada — cadastro manual ADMIN destrava o resto do produto (exibição pública, comparação de orçamento) sem essa dependência. Carga inicial de dados reais é uma tarefa de conteúdo/curadoria separada, não deste código |
| P-067 | ~~Catálogo de plantas — CRUD + vitrine pública (E9-05 parte 1 de 2)~~ | E9-05 | Resolvido (2026-08-04) | Checkout + comissão + marca d'água real implementados na parte 2 — ver P-068. `vendasCount` agora é incrementado a cada compra |
| P-068 | **Catálogo de plantas — checkout + marca d'água (E9-05 parte 2 de 2, completa o épico E9).** `POST /catalog/projects/:id/buy` (qualquer usuário autenticado — CLIENTE_PF/PJ, PRESTADOR, FORNECEDOR, TECNICO; arquiteto não pode comprar o próprio projeto) usa PSP SIMULADO (mesmo padrão E4/E7-04: `pspRef` sempre `SIMULADO-<uuid>`, sucesso sempre instantâneo, sem caminho de falha/recusa/estorno). Comissão via `CATALOG_COMMISSION_BPS` (placeholder 20%, dentro da faixa "15–25%" do PRD, não decisão fechada). Compra é única por (projeto, comprador) — banco impede duplicata (`@@unique`); comprar de novo devolve 409, não uma segunda cópia (sem suporte a "comprar de novo" pra presentear terceiros, por exemplo). **Marca d'água implementada de verdade** (`WatermarkService`, `sharp` p/ imagem — texto rotacionado sobreposto via SVG — e `pdf-lib` p/ PDF — texto desenhado em cada página): baixa o arquivo original (fetch da URL pública do S3), aplica a marca com o nome do comprador (`"ConectaObra — {nome}"`, funciona como rastro de vazamento) e reenvia sob uma key nova; validado fora do framework (`tsx` standalone, sem S3: PNG e PDF sintéticos, marca d'água aumenta o tamanho do arquivo mantendo dimensões/contagem de páginas). **Sem S3 configurado (P-018) ou tipo de arquivo não suportado** (`.dwg`/`.skp`/`.zip` etc.), a compra não é bloqueada — entrega o arquivo original sem marca d'água (`marcaDaguaAplicada: false`, confirmado via teste standalone). `GET /catalog/purchases` lista as compras do usuário com os links dos arquivos entregues. Sem UI em `apps/web` (nem botão de comprar na vitrine, nem tela de "minhas compras") | E9-05 | Aberto (comissão/PSP real bloqueados por P-002; watermarking real só roda de fato quando houver S3 configurado, que ainda não existe neste ambiente) | Watermarking fora da `$transaction` do Prisma de propósito (I/O de rede — download + upload no S3 — estouraria o timeout de transação interativa se estivesse dentro). Único por (projeto, comprador) é a decisão mais simples que cobre o caso de uso descrito (comprar uma planta pra usar); revisitar se "presentear"/relicenciar virar demanda real |
| P-069 | Telas de notícias + biblioteca em `apps/web` (E9-01/E9-04, 1ª rodada de telas do épico E9). `/noticias` (portal, lista `GET /public/articles`) + `/noticias/[slug]` (detalhe, `generateMetadata` pra SEO) + `/biblioteca` (reaproveita o mesmo endpoint, filtra client-side por `arquivoUrl != null` — convenção sem suporte no schema, ver P-065). **Sem forma de cadastrar conteúdo pela UI** — `apps/admin` ainda é só um scaffold vazio (README + `package.json`, nenhuma tela real), então `POST/PATCH/DELETE /articles` (ADMIN-only) só são alcançáveis via chamada direta à API; as tabelas ficam vazias em qualquer ambiente novo até isso ser feito manualmente ou o painel admin (E10-01, não iniciado) existir. Corpo do artigo renderizado como parágrafos simples (`split("\n")`) — sem markdown/rich text. **Corrigido de quebra nesta rodada**: `.claude/launch.json`/`apps/web/package.json` tinham uma mudança recente (troca de porta padrão pra 3399, sessão externa) que quebrava `next dev` (pnpm repassava um `--`/`-p`/`3399` malformado, `next dev` tentava interpretar "-p" como diretório do projeto) — corrigido fixando `-p 3399` direto no script `dev` do `package.json` em vez de via `runtimeArgs` do launch.json | E9-01, E9-04 (frontend) | Aberto | Painel ADMIN de conteúdo é escopo grande o suficiente (scaffold de app inteiro, E10-01) pra não ser resolvido de passagem aqui — fica registrado como próximo passo real quando o épico de admin for priorizado. Markdown/rich text no corpo do artigo é polimento que pode esperar conteúdo real existir |
| P-070 | Telas de indicadores + custos médios em `apps/web` (E9-02/E9-03, 2ª rodada de telas do épico E9). `/indicadores` (filtro GET por `tipo`/`regiao`, sem client JS, consome `GET /public/indicators`) + `/custos-medios` (filtro GET por `cidade`/`servico`, consome `GET /public/avg-costs`, exibe mín/médio/máx formatado em R$). Mesmo padrão de `/noticias`: Server Component, `ApiUnavailableError` tratado com fallback visual, sem paginação real (só `limit` fixo). **Mesma lacuna de P-069**: como o cadastro de `Indicator`/`AvgCost` é 100% manual via ADMIN (ver P-066) e não existe UI admin (E10-01), as duas páginas mostram "nenhum registro" em qualquer ambiente novo até alguém popular via chamada direta à API | E9-02, E9-03 (frontend) | Aberto | Mesma decisão de escopo de P-069 — painel ADMIN fica para quando E10-01 for priorizado |
| P-071 | **Telas de catálogo de plantas em `apps/web` (E9-05, 3ª e última rodada de telas do épico E9 — completa o épico E9 no frontend).** Vitrine pública `/catalogo` (filtro GET por categoria, `GET /public/catalog/projects`) + `/catalogo/[id]` (detalhe + botão "Comprar", `ComprarButton` client component chamando `POST /api/catalog/projects/:id/buy`; redireciona pro login se a sessão não existir — `401` do route handler). Aba "Catálogo" no painel do prestador (`CatalogoPanel`, mesmo padrão de `AdsPanel`/`PortfolioPanel`, CRUD completo, restrito a PRESTADOR/TECNICO igual o resto da página). Nova página `/compras` ("minhas compras", qualquer usuário autenticado — adicionada ao matcher do `middleware.ts`). **Sem upload de arquivo/imagem wired a nenhum formulário** (mesmo padrão de `fotos` em portfólio/produtos/RFQ, P-018): o campo `arquivos` do projeto (obrigatório, mín. 1) e `imagemCapaUrl` são preenchidos como URLs de texto puro (uma por linha pro primeiro, input único pro segundo) — sem um bucket S3 configurado neste ambiente, não há como testar um upload real de qualquer forma. Compra é PSP simulado (ver P-068) — o botão "Comprar" sempre funciona, nunca falha por recusa de pagamento | E9-05 (frontend) | Aberto (mesma dependência de S3/P-018 pro upload real; comissão/PSP real bloqueados por P-002) | Upload real de arquivo é um componente de UI reutilizável (usaria `POST /media/presigned-upload`, P-018) que vale a pena construir uma vez só e reaproveitar em portfólio/produtos/catálogo/RFQ juntos, não replicar aqui isoladamente — fica como próximo passo quando o upload de mídia for priorizado como tarefa própria. **Com isso, o épico E9 está completo em `apps/web`** (notícias, biblioteca, indicadores, custos médios, anúncios, catálogo de plantas) |

| P-072 | **Moderação de perfis — backend (E10-01, 1ª rodada do épico E10, painel admin).** Novo campo `User.suspenso`/`suspensoMotivo`/`suspensoEm` — **confirmado com o usuário via `AskUserQuestion`: suspender/reativar conta, sem histórico de moderação dedicado** (reaproveita `audit_log`, já append-only, em vez de uma tabela própria). Novo `AdminModule`: `GET /admin/users?tipo=&q=&suspenso=&limit=` (busca por nome/e-mail), `GET /admin/users/:id`, `PATCH /admin/users/:id/suspender` (motivo obrigatório, min 3 caracteres) e `PATCH /admin/users/:id/reativar` — todos `ADMIN`-only. Suspender revoga todas as sessões (`TokenService.revokeAllForUser`, mesmo padrão de `AccountService.deleteAccount`/E1-08) e bloqueia `POST /auth/login`/`POST /auth/refresh` (`ForbiddenException`) — mas **não revoga um access token já emitido** (mesmo trade-off de P-020: token de 15min continua válido até expirar naturalmente). ADMIN não pode suspender a própria conta. Visão do ADMIN inclui `cpfCnpj`/`telefone` (necessário pra identificar o usuário na moderação) mas nunca `senhaHash`/`mfaSecret`. Disputas (E4-09/10) e conteúdo (notícias/indicadores/custos médios, E9-01/02/03) **já têm endpoint ADMIN pronto desde antes** — este painel só vai consumi-los, sem backend novo pra essas duas partes. Sem UI ainda (`apps/admin` continua scaffold vazio) — ver próxima rodada | E10-01 | Aberto | Suspensão idempotente é bloqueada por `ConflictException` (já suspenso/já excluído) em vez de silenciosamente virar no-op — evita um ADMIN achar que reenviou um motivo diferente quando na verdade nada mudou. `apps/admin` (Next.js, porta 3001) — confirmado com o usuário: espelhar o padrão de sessão de `apps/web` (cookies httpOnly próprios, login restrito a ADMIN) em vez de compartilhar cookie entre os dois apps |

| P-073 | **`apps/admin` deixou de ser scaffold vazio — app Next.js real (E10-01, 2ª rodada do épico E10).** Login (`/entrar`) restrito a `ADMIN`: o Route Handler `POST /api/auth/login` rejeita qualquer outro `tipo` **antes** de emitir cookie de sessão (403, mesmo se a senha estiver correta), e a página `/` (dashboard) faz a mesma checagem de novo do lado do servidor (defesa em profundidade). Cookies de sessão têm nomes **distintos** dos de `apps/web` (`co_admin_access_token`/`co_admin_refresh_token`, não `co_access_token`/`co_refresh_token`) — necessário porque em dev os dois apps rodam em `localhost` (portas diferentes) e cookies ignoram porta no escopo; usar o mesmo nome faria a sessão de um vazar/colidir com a do outro no navegador. `middleware.ts` protege tudo por padrão (matcher nega só `/entrar`, `/api/auth/*` e assets do Next) — inverso do padrão de `apps/web` (majoritariamente público com poucas rotas protegidas), já que quase todo o admin exige sessão. Dashboard (`/`) é só um shell com saudação + 3 cards placeholder (Usuários/Disputas/Conteúdo, "em breve") — nenhuma tela funcional ainda. `.claude/launch.json` ganhou entrada `admin` (porta 3001, confirmada com o usuário/já documentada em `docs/prd/03_Estrutura_Projeto.md`). Validado no browser: login sem sessão redireciona pra `/entrar`, submeter credenciais sem backend mostra "serviço indisponível" corretamente, sem erro de console. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam | E10-01 | Aberto | Próximas rodadas: tela de usuários (busca/detalhe/suspender/reativar, backend 100% pronto desde P-072), fila de disputas (backend pronto desde E4-09/10) e CRUD de conteúdo (notícias/indicadores/custos médios, backend pronto desde E9-01/02/03) — nenhuma delas precisa de endpoint novo, só consumir o que já existe |

| P-074 | **Tela de usuários no `apps/admin` (E10-01, 3ª rodada do épico E10).** `/usuarios` (filtro GET por tipo/nome-e-mail/suspenso, consome `GET /admin/users`) + `/usuarios/:id` (detalhe completo + `SuspendForm`: suspender exige motivo mín. 3 caracteres, reativar é um clique com confirmação). Conta excluída (`deletedAt`) não mostra o formulário de suspender/reativar (não faz sentido moderar uma conta já anonimizada). Porta do `apps/admin` trocada de 3001 para **3096** (pedido do usuário) — atualizada em `apps/admin/package.json` (script `dev`), `.claude/launch.json`, `apps/admin/README.md` e `docs/03_Estrutura_Projeto.md`/`docs/prd/03_Estrutura_Projeto.md`. Card "Usuários" no dashboard agora linka de verdade pra `/usuarios`; "Disputas"/"Conteúdo" continuam placeholder. Validado no browser: `/usuarios` e `/usuarios/:id` redirecionam pro login sem sessão (middleware cobre por padrão, sem precisar de matcher novo), sem erro de console. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam | E10-01 | Aberto | Próximas rodadas: fila de disputas e CRUD de conteúdo (notícias/indicadores/custos médios) — backend de ambos já existe, mesmo padrão desta rodada |

| P-075 | **Fila de disputas no `apps/admin` (E10-01, 4ª rodada do épico E10) — completa E4-09/E4-10 com UI.** `GET /disputas` (já exclusivo do ADMIN desde E4-09) ganhou contexto: `DisputesService.listAbertas()` agora inclui obra/etapa/valor/nome de quem abriu via novo tipo `AdminDispute` (`packages/types/src/disputes.ts`) — sem esse contexto, o mediador não tinha como decidir "aprovar/estornar/liberar parcial" com segurança (não dava pra saber o valor em disputa nem a obra). `listForMilestone` (visão das partes do contrato) continua em `DisputePublic`, sem mudança — a rota de disputas do ADMIN é a única exclusiva o suficiente pra mudar de forma sem quebrar outro consumidor. Nova página `/disputas`: lista disputas abertas com motivo/evidências/contexto + `ResolveForm` (decisão + resolução obrigatória + valor condicional pra liberação parcial). Novo Route Handler `/api/disputas/:id/resolver`. Card "Disputas" no dashboard agora linka de verdade. Validado: `tsc`/`nest build`/boot-test (só P1001)/`next build`, redirect de sessão confirmado no browser, sem erro de console. `pnpm build`/`lint`/`test` da raiz passam | E10-01, E4-09, E4-10 | Aberto | Resolução financeira (estornar/liberar parcial) ainda passa pelo `EscrowService` já existente, sem mudança de lógica — só a leitura ganhou contexto. Sem paginação na fila (mesma decisão de escopo do resto do painel admin) |

| P-076 | **CRUD de conteúdo no `apps/admin` (E10-01, 5ª e última rodada do épico E10 — completa o painel admin: usuários + disputas + conteúdo).** `/conteudo` (landing) + `/conteudo/artigos` (CRUD completo de notícias/biblioteca via `GET/POST/PATCH/DELETE /articles`, já existentes) + `/conteudo/indicadores` e `/conteudo/custos-medios` (lista via endpoint público sem login + formulário de upsert, já existentes — `POST /indicators`/`POST /ai/avg-costs`). **Indicadores e custos médios não têm exclusão** — o backend nunca teve `DELETE` pra nenhum dos dois (só upsert desde E5-05/E9-02), então a UI reflete essa limitação: "Editar" preenche o formulário com os valores existentes (upsert por chave, não por id) em vez de navegar pra uma rota `/:id`; não há botão de excluir porque a ação não existe no backend. Formulários usam remount por `key` (não a opção `values` do react-hook-form) especificamente pra preservar a conversão centavos↔reais na exibição — usar `values` bugaria mostrando o valor em centavos cru no campo de reais. Card "Conteúdo" no dashboard agora linka de verdade. Validado no browser: as 4 páginas (`/conteudo` + 3 filhas) redirecionam pro login sem sessão, sem erro de console. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam | E10-01 | Aberto (exclusão de indicadores/custos médios bloqueada por ausência de endpoint — mesma categoria de decisão de escopo, não fechada) | Adicionar `DELETE /indicators/:id` e `DELETE /ai/avg-costs/:id` é uma tarefa de backend pequena e isolada se a exclusão vier a ser necessária — não implementado agora pra não expandir escopo além do que a rodada pedia (consumir o que já existe). **Com isso, o painel admin (E10-01) está completo**: moderação de perfis, fila de disputas, CRUD de conteúdo |

| P-077 | **Testes E2E do fluxo crítico completo + carga do matching (E10-04) — primeira suíte de testes de verdade do monorepo (todo `"test"` era um echo stub até aqui).** `apps/web/e2e/critical-flow.spec.ts` (Playwright, novo devDependency) cobre o loop central do CLAUDE.md: cadastro (cliente + prestador) → obra → RFQ → matching → proposta → aceite → contrato → milestone → depósito em escrow → entrega → aprovação → liberação automática → extrato financeiro. **Confirmado com o usuário via `AskUserQuestion`: abordagem híbrida** — navegador só nas duas únicas telas reais que existem nesse trecho do fluxo (`/cadastro` de cliente e de prestador); todo o resto (obra, RFQ, matching, proposta, aceite, milestone, escrow, extrato) é chamada direta a `services/api`, porque **nenhuma dessas telas existe em `apps/web`** (RFQ: a própria `/rfq` diz "use POST /rfq direto na API"; escrow/milestone: só `GET .../financeiro` e o ledger cru, sem UI — P-041/P-047). `POST /proposals/:id/accept` em particular só devolve o `Contract` na resposta da própria chamada (sem endpoint de consulta depois), então mesmo o botão "Aceitar" que já existe (`accept-proposal-button.tsx`) não seria suficiente pra encadear os próximos passos do teste. Gerador de CPF válido (dígito verificador real) testado isoladamente fora do framework (`node -e`, 10.000 amostras, 0 falhas contra o mesmo algoritmo de `documents.ts`). k6 (`services/api/test/load/matching-k6.js`) mede o custo de `MatchingService.matchRfq()` sob carga crescente (10 VUs, thresholds de p95<800ms e taxa de erro<1%). **Nenhum dos dois nunca rodou de verdade** — sem Docker/Postgres/Redis neste ambiente (mesma limitação de sempre) e sem o binário do k6 instalado. Validado só até onde dá: `tsc` (`apps/web` inclui `e2e/**/*.ts` no mesmo `tsc --noEmit`), `next build` (confirma que `e2e/` não vaza pra rotas do app), `playwright test --list` (parseia e enumera o teste corretamente) | E10-04 | Aberto (nunca executado contra a stack real — bloqueado pela mesma ausência de Docker de sempre) | Escrito e revisado com precisão de contrato (rotas/schemas conferidos um a um nos controllers/DTOs reais, não inventados) — o risco residual é de comportamento em runtime que só um ambiente real revelaria (timing do matching best-effort, por exemplo). Primeira vez que o projeto ganha testes reais (não mais stubs `echo ok`) — os scripts `test` de `apps/web`/`services/api` continuam sendo os stubs de sempre; `test:e2e` é um script novo e separado, não plugado no pipeline `test` do Turborepo (rodar E2E exige a stack inteira de pé, não cabe no `pnpm build/lint/test` rápido de todo commit) |

| P-078 | **Cadastro assistido de prestadores no `apps/admin` (E10-02) — ferramenta de campo pra onboarding dos 200 prestadores-piloto.** `/prestadores-piloto`: um único formulário combina conta (`POST /auth/register`, `tipo: PRESTADOR`) + perfil (`PUT /profile/prestador`) numa só chamada orquestrada pelo Route Handler `POST /api/prestadores-piloto` — sem isso, o agente precisaria pedir pro prestador se cadastrar sozinho depois, o que não tem como funcionar sem SMS/e-mail de convite real (P-006/notificações continuam stub). O Route Handler usa o token do **prestador recém-criado** (devolvido por `/auth/register`) só server-side pra chamar `/profile/prestador` — nunca expõe esse token ao browser do admin. Contador "X/200" reaproveita `GET /admin/users?tipo=PRESTADOR&limit=500` (já existente desde P-072) contando o array — sem endpoint de contagem dedicado. **Se a conta é criada mas o perfil falha ao salvar** (rede caiu no meio, etc.), a resposta é HTTP 207 e a conta fica sem perfil — não há como desfazer o cadastro via admin (exclusão de conta exige a senha do próprio usuário, E1-08), então o agente vê um aviso e precisa completar o perfil depois (login do prestador ou nova chamada a `/profile/prestador`, ainda sem UI pra isso fora do próprio prestador logado). Card "Cadastro assistido" no dashboard. Validado no browser: `/prestadores-piloto` redireciona pro login sem sessão, sem erro de console. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam | E10-02 | Aberto (fluxo de "completar perfil depois de um 207" não tem UI — casos raros, não implementado) | Escopo confirmado pelo próprio texto do backlog (só prestadores-piloto, não fornecedores) — extensão pra fornecedor fica pra quando for pedida. Senha é digitada pelo agente (sem geração automática nem link de redefinição — nenhum dos dois existe no produto ainda) |

| P-079 | **Analytics de funil + dashboard de KPIs — backend (E10-03, 1ª rodada).** Duas peças distintas, como o próprio título do backlog descreve: **(1)** `AnalyticsService` (PostHog, `posthog-node`) — fornecedor em aberto (mesma categoria de S3/PSP/SMS): sem `POSTHOG_API_KEY`, `capture()` só descarta o evento, nunca bloqueia o fluxo que o chama (mesmo padrão de `MediaService`/E1-07). Instrumentado nos pontos reais do funil de negócio: `user_registered` (`AuthService.register`), `rfq_created` (`RfqService.create`), `rfq_proposal_submitted` (`RfqProposalService.submit`), `rfq_proposal_accepted` (`ContractsService.acceptProposal`), `escrow_deposited` (`EscrowService.depositar`), `milestone_paid` (`MilestonesService`, só quando o escrow libera de fato — é o evento mais próximo do North Star do PRD, GMV via Hub Financeiro). **(2)** `GET /admin/kpis` (ADMIN-only, `AdminKpisService`) — os 6 KPIs da tabela do `01_PRD` §3 calculados **direto do Postgres** (determinístico, CLAUDE.md §5 regra 3), **não depende do PostHog estar configurado**: liquidez (% RFQs respondidos em 24h), confiança (disputas/transações — "transação" interpretado como `Contract`, PRD não define o termo com precisão, decisão documentada aqui), receita (MRR de assinaturas), adoção de IA (% clientes com ≥3 perguntas/semana, reaproveitando a mesma lógica de contagem de `AiChatService.enforceDailyLimit`/E5-06), churn (sempre `null` — sem billing real, E8, não existe evento de cancelamento pra medir) e ativação (% clientes que criaram obra E publicaram RFQ em 7 dias). MRR/churn sempre reportam 0/null em qualquer ambiente hoje — honesto, não é bug: nenhum código cria uma `Subscription` ainda (E8 não implementado). Lógica pura de contagem (adoção IA, janela de ativação) validada isolada (`node -e`, casos de borda cobertos). Sem UI ainda — ver próxima rodada. `tsc`/`nest build`/boot-test (só P1001)/`pnpm build`/`lint`/`test` da raiz passam | E10-03 | Aberto (PostHog nunca testado com API key real; KPIs nunca testados contra dado real — sem Postgres neste ambiente) | Consulta de liquidez/ativação usa `Promise.all` de queries por RFQ/cliente em vez de uma única query agregada com SQL bruto — aceitável na escala esperada do piloto (~200 prestadores, poucas centenas de RFQs), revisitar com SQL agregado se o volume crescer |

| P-080 | **Analytics de funil + dashboard de KPIs — frontend (E10-03, 2ª e última rodada, completa o épico E10-03).** `/kpis` no `apps/admin`: os 6 KPIs do PRD em cards (valor atual + meta + badge "na meta"/"abaixo da meta"), consumindo `GET /admin/kpis` (ver P-079). PostHog client-side em `apps/web`: `lib/posthog.ts` (init preguiçoso, só se `NEXT_PUBLIC_POSTHOG_KEY` existir — sem ela, `getPostHog()` sempre devolve `null`), `<PostHogPageview>` no layout raiz (dispara `$pageview` a cada troca de rota do App Router — `posthog-js` não escuta isso sozinho numa SPA) + eventos `cadastro_concluido`/`login_concluido` nos dois únicos formulários reais do funil inicial. **Achado de build**: `getPostHog()` importado diretamente nas páginas de cadastro/entrar fez o bundler duplicar `posthog-js` no chunk de cada uma (~254kB First Load JS, contra ~176kB antes) em vez de deduplicar com o chunk do `PostHogPageview` do layout — funciona, mas não é o bundling ideal; revisitar se o tamanho incomodar de verdade. Validado no browser: `/cadastro`/`/entrar` renderizam sem erro de console (PostHog não configurado, tudo no-op), `/kpis` redireciona pro login sem sessão. `tsc`/`next build` (dois apps)/`pnpm build`/`lint`/`test` da raiz passam — nunca testado com PostHog configurado de verdade nem com dado real no Postgres | E10-03 | Aberto (sem API key real do PostHog; KPIs nunca calculados contra dado real) | Deduplicação de bundle do posthog-js é polimento de performance, não bloqueia nada — revisitar só se o tamanho da página virar reclamação real. **Com isso, o épico E10-03 está completo** |

## 🟢 DÍVIDAS TÉCNICAS / MELHORIAS (não bloqueiam)

| ID | Item | Contexto |
|---|---|---|
| D-001 | Avaliar tRPC vs REST puro para contratos web↔api | Decidir até Sprint 2; registrar ADR |
| D-002 | ~~Estratégia de testes de carga do matching (k6)~~ | Script escrito (`services/api/test/load/matching-k6.js`) — nunca executado neste ambiente (sem k6/Docker). Ver P-077 |
| D-003 | Wireframes → protótipo navegável de alta fidelidade no Figma | Após validação dos fluxos com 5 usuários |

## ✅ CONCLUÍDAS

| Data | Item |
|---|---|
| 2026-07-24 | Pacote de kickoff (PRD, spec, estrutura, backlog, checklists, wireframes) criado em `docs/prd/` |
| 2026-07-24 | Branch `sprint-0` criada com scaffold do monorepo (S0-01 parcial) |
| 2026-07-24 | Docker Compose local: Postgres 16 + PostGIS + pgvector, Redis, Meilisearch (S0-02 parcial) |
| 2026-07-24 | Workflow de CI (lint/test/build) criado (S0-03 parcial) |
| 2026-07-24 | `CLAUDE.md` e `PENDENCIAS.md` criados na raiz |
| 2026-07-27 | `pnpm install` executado com sucesso na raiz — `pnpm-lock.yaml` gerado e commitado (parcial de P-007) |
| 2026-07-27 | Design System v0 (S0-04, parcial): tokens + preset Tailwind + 15 componentes base em `packages/ui`, branch `feat/S0-04-design-system-v0` — ver P-009 |
| 2026-07-27 | Schema Prisma inicial (S0-05, parcial): 24 modelos/enums traduzidos do doc 02 §3, migração inicial + trigger append-only (`escrow_transactions`, `audit_log`) e seed de desenvolvimento, branch `feat/S0-05-prisma-schema` — ver P-010 |
| 2026-07-27 | Validação de env com zod em `packages/config` (`baseEnvSchema`/`parseEnv`), consumida por `services/api` |
| 2026-07-27 | Observabilidade v0 (S0-06, parcial): bootstrap NestJS mínimo (`main.ts`, `app.module.ts`, `GET /health`), logger estruturado (`nestjs-pino` com redact LGPD), `SentryExceptionFilter` global e `AuditLogService` (sanitiza CPF/token antes do insert), branch `feat/S0-06-observabilidade` — ver P-011. Bootstrap completo (env → Nest → logger → Prisma) testado de ponta a ponta com `tsc --noEmit`, `nest build` e execução real; única falha foi a esperada por não haver Postgres no ambiente |
| 2026-07-27 | DTOs zod compartilhados em `packages/types` (`@conectaobra/types/auth`, `/documents`), incluindo validação de CPF/CNPJ por dígito verificador (testada com casos conhecidos) |
| 2026-07-27 | Cadastro/login + refresh token + OTP telefone (E1-01, parcial): `AuthModule` completo em `services/api/src/modules/identity/auth/`, branch `feat/E1-01-cadastro-login-work` — ver P-014 |
| 2026-07-27 | Onboarding por perfil (E1-02, parcial): `ProfileModule` (`GET /profile/me`, `PUT /profile/{prestador,fornecedor}`) + `UserTypeGuard`, branch `feat/E1-02-onboarding-perfil` — ver P-015 |
| 2026-07-27 | ~~P-016~~ Push ao GitHub resolvido: usuário configurou credencial (PAT) via `osxkeychain` com um `git push` manual; a partir daí, pushes desta sessão (S0-04, S0-05, S0-06, E1-01, E1-02) funcionaram normalmente, inclusive os PRs sugeridos pelo GitHub no retorno do push |
| 2026-07-27 | MFA/TOTP (E1-04, parcial): `POST /auth/mfa/{setup,enable,disable,verify-login}`, branch `feat/E1-04-mfa` — ver P-017 |
| 2026-07-28 | Upload de mídia (E1-07, parcial): `POST /media/presigned-upload` (S3), branch `feat/E1-07-upload-midia` — ver P-018 |
| 2026-07-28 | LGPD (E1-08, parcial): consentimentos append-only (`GET/POST /legal/*`) + exclusão de conta por anonimização (`DELETE /account`), branch `feat/E1-08-lgpd` — ver P-019 |
| 2026-07-28 | Code review completo da sessão (main → feat/E1-08-lgpd): 2 bugs de atomicidade corrigidos (`register()`/`deleteAccount()` agora em `$transaction`) e 1 limitação documentada (P-020) |
| 2026-07-28 | Criar obra (E3-01, parcial): `WorksModule` (`POST/GET/GET:id/PATCH /works`), branch `feat/E3-01-criar-obra` — ver P-021 |
| 2026-07-28 | Publicar RFQ (E3-02, parcial): `RfqModule` (`POST/GET/GET:id/PATCH /rfq`), branch `feat/E3-02-publicar-rfq` — ver P-022 |
| 2026-07-28 | **Merge de toda a sessão em `main`**: `S0-04 → S0-05 → S0-06 → E1-01 → E1-02 → E1-04 → E1-07 → E1-08 → E3-01 → E3-02` (fast-forward da pilha principal + merge do S0-04 independente, único conflito foi em PENDENCIAS.md, resolvido mantendo a versão mais atualizada). Validação completa pós-merge (`tsc`/`nest build`/build de todos os pacotes/execução real) achou e corrigiu 1 bug real: `packages/ui/tailwind.preset.ts` não tipava contra o `tailwindcss` de verdade (nunca fora buildado antes por falta de `pnpm install`) — `fontFamily.sans` é uma tupla `readonly`, incompatível com o `string[]` mutável que o `Config` do Tailwind espera |
| 2026-07-28 | Motor de matching regional (E3-03, parcial): `MatchingService` (categoria + raio PostGIS + rodízio) integrado em `RfqService.create()` + `GET /rfq/discover`, branch `feat/E3-03-matching` — ver P-024 |
| 2026-07-28 | Envio de proposta (E3-05, parcial): `RfqProposalService` (`POST/GET /rfq/:id/proposals`, cap mensal placeholder do plano gratuito), branch `feat/E3-05-enviar-proposta` — ver P-025 |
| 2026-07-28 | Aceitar proposta → rascunho de contrato (E3-07, parcial): `ContractsModule` (`POST /proposals/:id/accept`), branch `feat/E3-07-aceitar-proposta` — ver P-026 |
| 2026-07-28 | Notificações de RFQ via fila (E3-04, parcial): `NotificationsModule` (BullMQ) integrado em `MatchingService`, branch `feat/E3-04-notificacoes` — ver P-027. Com isso, o épico E3 (obra→RFQ→matching→proposta→aceite→contrato→notificação) está com todas as tasks P0/P1 implementadas, exceto E3-06 (comparador — mais front-end, `apps/web` ainda não existe) |
| 2026-07-28 | Code review de E3-03/E3-05/E3-07/E3-04: 2 races corrigidas (`@@unique([rfqId, proponenteId])` em `RfqProposal` + `P2002` tratado; `ContractsService.acceptProposal()` agora valida `status ABERTO`/`ENVIADA` com `updateMany` condicional DENTRO da `$transaction`, fechando o double-accept concorrente) e 1 limitação de baixa severidade documentada (P-028) |
| 2026-07-28 | **Merge de E3-03/E3-05/E3-07/E3-04 em `main`** (fast-forward, sem conflito). `main` validado de ponta a ponta de novo pós-merge (`pnpm install`, `tsc`, `nest build`, execução real) |
| 2026-07-28 | **`apps/web` (E1-01, front-end) criado do zero** em `feat/E1-01-web-auth`, a partir de `main`: Next.js 15 (App Router) + Tailwind com o preset de `packages/ui` (S0-04) + `react-hook-form`/`@hookform/resolvers/zod` contra os schemas de `packages/types/auth`. Landing page (`/`), cadastro (`/cadastro`) e login (`/entrar`) com validação client-side em PT-BR, Route Handlers (`/api/auth/{register,login,logout,refresh}`) fazendo proxy pro `services/api` e guardando os tokens em cookies `httpOnly` (`lib/session.ts`) — o browser nunca vê o JWT. `middleware.ts` protege `/conta/*` redirecionando pro login com `?redirect=`. Área logada mínima (`/conta`, Server Component) busca `GET /profile/me` com o access token do cookie e tem botão de logout. Build (`next build`) e `tsc --noEmit` passam; testado no browser (preview local, sem `services/api` rodando): formulários validam certo, e as chamadas às Route Handlers devolvem 502 com mensagem PT-BR em vez de estourar erro 500 genérico — ver P-029/P-030 pelo que ficou de fora |
| 2026-07-28 | Code review do front (achado + corrigido na mesma sessão): mensagens de erro do Zod em `registerInputSchema`/`loginInputSchema` (`nome`, `email`, `senha` do login) vazavam em inglês ("Invalid email", "String must contain..."), violando a regra de UI 100% PT-BR do `CLAUDE.md` §5 — adicionadas mensagens customizadas em `packages/types/src/auth.ts`. Também corrigido: os Route Handlers de auth e o Server Component `/conta` não tratavam falha de conexão com `services/api` (ECONNREFUSED gerava erro 500 não tratado) — agora usam `apiFetchOrThrow`/`ApiUnavailableError` (`lib/api-client.ts`) e devolvem 502 com mensagem amigável |
| 2026-07-28 | Segunda passada de review antes do merge: `?redirect=` do `/entrar` (usado pelo `middleware.ts` pra voltar o usuário pra rota protegida após o login) era repassado direto pro `router.push()` sem validação — um link tipo `/entrar?redirect=https://evil.com` era um open redirect em potencial após login bem-sucedido. Corrigido com `safeRedirect()` (`apps/web/lib/safe-redirect.ts`), que só aceita caminho relativo interno (`/conta`, `/x`), rejeitando URL absoluta ou protocol-relative (`//evil.com`) |
| 2026-07-28 | **Merge de `feat/E1-01-web-auth` em `main`** (fast-forward, sem conflito) — `apps/web` (landing, cadastro, login, área logada `/conta`) agora faz parte de `main` |
| 2026-07-28 | `DEPLOY.md` + `deploy.sh` criados: primeira instalação e atualizações numa VPS Ubuntu própria (Nginx e Postgres já instalados nativamente, sem Docker/PaaS em produção — decisão confirmada com o time nesta sessão). `services/api` fica só acessível internamente (127.0.0.1:3333), nunca exposto direto à internet; só `apps/web` fica atrás do Nginx. PM2 gerencia os dois processos (`infra/deploy/ecosystem.config.cjs`); `infra/deploy/nginx.conf.example` é o server block de referência. `deploy.sh` faz git pull + build + `prisma migrate deploy` + `pm2 reload` + checagem de `/health`, e recusa rodar com o working tree sujo. **Não testado contra uma VPS real** (sem acesso a uma nesta sessão) — só validado localmente (`bash -n`, `node -e require(...)` no ecosystem file). Ver P-031 |
| 2026-07-28 | Bug real achado ao validar o boot de `services/api` com `pnpm start`: o script apontava pra `dist/main.js`, mas o `nest build` gera `dist/src/main.js` (rootDir inclui `src/` e `prisma/`) — `pnpm start` sempre teria falhado (`MODULE_NOT_FOUND`) em qualquer deploy real, mesmo local. Nunca foi pego antes porque toda validação anterior desta sessão rodava `node dist/src/main.js` direto, nunca via `pnpm start`. Corrigido em `services/api/package.json`, `infra/deploy/ecosystem.config.cjs` e `DEPLOY.md` — reconfirmado com `pnpm start` rodando até o ponto esperado (`PrismaClientInitializationError: Can't reach database server`, ambiente sem Postgres) |
| 2026-07-28 | Bug real achado ao rodar `pnpm lint` na raiz pela primeira vez nesta sessão: `packages/ui` usava `eslint src`, mas `eslint` nunca foi instalado como dependência (pré-existente desde S0-04) — `pnpm lint` da raiz inteira estava quebrado o tempo todo (`sh: eslint: command not found`), provavelmente deixando o step de lint do CI (`.github/workflows/ci.yml`) vermelho sem ninguém notar, já que cada task anterior só rodava `tsc --noEmit`/`nest build`/`next build` isoladamente, nunca `pnpm lint` da raiz. Corrigido trocando pra `tsc --noEmit`, mesmo padrão do resto do monorepo (mesmo ajuste já feito em `apps/web` na sessão anterior por causa do `next lint` interativo) |
| 2026-07-28 | Comparador de propostas (E3-06, parcial): backend ganhou `proponenteNome` em `RfqProposalPublic` (`packages/types/src/rfq-proposals.ts` + `toPublicRfqProposal` + `RfqProposalService`, via `include: {proponente: {select: {nome:true}}}`) — sem isso o front só teria um UUID pra mostrar. `apps/web`: `/rfq` (lista "minhas RFQs"), `/rfq/[id]` (detalhe + tabela responsiva comparando preço/prazo/observações/status, com destaque determinístico "Menor preço"/"Mais rápido" — não é IA, rotulado explicitamente como tal), botão "Aceitar" com confirmação (Client Component) chamando o novo Route Handler `POST /api/proposals/[id]/accept`. `middleware.ts` estendido pra proteger `/rfq/*`. Refatorado `lib/auth-session.ts` (`requireAccessToken`) reaproveitado por `/conta` e pelas novas páginas. Escopo deliberadamente não cobre: criação de RFQ pela UI (E3-02 front ainda não existe) nem tela do prestador (ver P-032). `tsc`/`next build`/`nest build` passam; testado no browser que o middleware redireciona `/rfq` pro login corretamente — o restante do fluxo autenticado não pôde ser testado visualmente por falta de Postgres real e por não dar pra forjar o cookie `httpOnly` de sessão sem login de verdade |
| 2026-07-28 | **Merge de `feat/E3-06-comparador` em `main`** (fast-forward, sem conflito). `pnpm build`/`pnpm lint`/`pnpm test` da raiz inteira (6 workspaces) passam limpos pós-merge |
| 2026-07-28 | Calculadoras determinísticas (E5-04, parcial): `AiModule` novo em `services/api/src/modules/ai/` — `POST /ai/calc/{concreto,blocos,argamassa,tinta}`, endpoints públicos (sem dado sensível, cobertos pelo rate-limit global). `CalculatorsService` com funções puras: traço de concreto (método de partes + fator de conversão ~1,53), blocos por m² (geometria: área do bloco + junta), argamassa de assentamento (geometria: volume assentado − volume do bloco, não coeficiente fixo), tinta (área × demãos ÷ rendimento informado). Toda resposta inclui `disclaimer` (CLAUDE.md §5 regra 3 — tema estrutural exige recomendação de profissional habilitado). Testado com valores conhecidos fora do framework (`node -e` chamando o service direto): resultados batem com faixas comumente publicadas (blocos ~12,5/m², argamassa ~11L/m² pra bloco 9x19x39 com junta 1cm). **Coeficientes pendentes de revisão por engenheiro civil** — ver P-033/P-012. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-28 | **Merge de `feat/E5-04-calculadoras` em `main`** (fast-forward, sem conflito) |
| 2026-07-28 | Catálogo de produtos do fornecedor (E1-06/E2-06, parcial): `CatalogModule` novo em `services/api/src/modules/catalog/` — `POST/GET/GET:id/PATCH/DELETE /products`, restrito a `FORNECEDOR` dono (mesmo padrão 404-pra-quem-não-é-dono de `works`/`rfq`). O model `Product` já existia desde S0-05 (nunca tinha CRUD); `create()` agora checa que o `ProfileFornecedor` existe antes de inserir, senão a FK do banco quebraria com erro cru em vez de uma mensagem amigável. Escopo não cobre: vitrine pública (E2-03) nem busca/descoberta (E2-01/E2-02) — isso é catálogo de gestão do próprio fornecedor, sem tela nem endpoint público ainda. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-28 | **Merge de `feat/E1-06-catalogo-produtos` em `main`** (fast-forward, sem conflito) |
| 2026-07-28 | Portfólio de obras anteriores do prestador (E1-05, parcial): novo model `PortfolioItem` (título, descrição, fotos[], FK pra `profiles_prestador`) — migração escrita à mão seguindo as convenções do Prisma (mesmo padrão de toda migração desta sessão, sem Postgres real disponível pra gerar via `prisma migrate dev`), validada com `prisma validate`/`prisma generate` (schema-only, não precisa de conexão real). `POST/GET/PATCH/DELETE /profile/prestador/portfolio[/:id]` no `ProfileModule` existente, restrito a `PRESTADOR`/`TECNICO` dono. `create()` valida que o `ProfilePrestador` existe antes de inserir (mesmo padrão do catálogo de produtos — evita erro cru de FK). Com isso, E1-05 cobre os 5 itens do backlog (fotos, obras, certificados, experiência, raio — os 3 últimos já vinham de E1-02). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam; migração em si **nunca foi aplicada a um Postgres real** (mesma limitação de todas as migrações desta sessão, ver P-010) |
| 2026-07-28 | **Merge de `feat/E1-05-portfolio-prestador` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Indexação e busca via Meilisearch (E2-01/E2-02, parcial): `MEILI_HOST`/`MEILI_API_KEY` obrigatórios em `env.ts` (infra já decidida, mesmo status do `REDIS_URL`) — client `meilisearch@0.60.0` instalado, mas é preguiçoso (`new Meilisearch(...)` não conecta), então a ausência de um servidor real não derruba o boot. Novo `SearchModule`: `MeilisearchService` é um wrapper best-effort (nenhum método público propaga erro, mesmo padrão do enfileiramento BullMQ em `MatchingService`) sobre 3 índices (`prestadores`, `fornecedores`, `produtos`); `GET /search/{prestadores,fornecedores,produtos}` com filtros por categoria/região. `ProfileService.upsertPrestador`/`upsertFornecedor` e `ProductsService.create/update/remove` agora sincronizam o índice depois de cada escrita no Postgres. Resiliência a Meilisearch indisponível **testada isoladamente fora do Nest** (`node -e` instanciando `MeilisearchService` direto contra `localhost:7700`, que não existe neste ambiente) — confirmado que `onModuleInit`/`indexPrestador`/`searchPrestadores` nunca lançam exceção, só logam warning e devolvem fallback seguro (`[]` na busca). Isso não pôde ser confirmado via boot completo da API porque o `PrismaService` já quebra antes por falta de Postgres real (mesma limitação de sempre, ver P-010). Escopo não cobre: busca pública sem login (P-034) nem geo-busca combinada com texto (P-035). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E2-01-busca-meilisearch` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Avaliações pós-contrato (E2-04, parcial): `@@unique([contratoId, avaliadorId])` em `Review` (migração manual, mesmo padrão de sempre) — impede a mesma pessoa avaliar o mesmo contrato duas vezes, tratado como 409 amigável (P2002). `POST/GET /contracts/:contractId/reviews` (`ContractReviewsController`) e `GET /reviews/received` (`MyReviewsController`), ambos novos em `ContractsModule`. `ReviewsService.create()` nunca aceita `avaliadoId` do cliente — descobre sozinho quem é a outra parte do contrato via `ContractParty`; 404 se o requisitante não for parte do contrato (não vaza existência). **Limitação deliberada**: qualquer contrato existente permite avaliação, não há checagem de "serviço concluído" — `Contract.status` ainda não tem esse estado definido (depende de E6). Registrado como P-036. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E2-04-avaliacoes` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Nota agregada no perfil (E2-05, parcial): `ReviewsService.create()` agora recalcula `notaMedia` (média simples de prazo/qualidade/preço de todas as reviews recebidas) e grava em `ProfilePrestador.notaMedia`/`ProfileFornecedor.notaMedia` (`updateMany`, seguro mesmo se o avaliado for um CLIENTE sem nenhum dos dois perfis) — depois reindexa no Meilisearch via `MeilisearchService.updatePrestadorNota`/`updateFornecedorNota`, novos métodos que usam `updateDocuments` (merge parcial, diferente de `addDocuments` que substitui o documento inteiro). Resiliência a Meilisearch indisponível testada isoladamente de novo (`node -e`), mesmo padrão do E2-01. **"Selo básico" não implementado** — registrado como P-037, com o porquê (as únicas definições de selo nos docs dependem de KYC bloqueado ou são escopo do job de ranking de E8-04). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E2-05-nota-agregada` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Páginas públicas de prestador/fornecedor (E2-03, parcial): novo `PublicProfilesModule` (`services/api/src/modules/public-profiles/`) — `GET /public/{prestadores,fornecedores}/:id`, **primeiro controller da API sem `JwtAuthGuard`**, filtrando `deletedAt: null` e só devolvendo campos seguros (nunca e-mail/telefone/CPF-CNPJ/kycStatus); ainda coberto pelo rate-limit global do `ThrottlerGuard`. `apps/web`: `/prestadores/[id]` e `/fornecedores/[id]`, Server Components sem `requireAccessToken` (de propósito — visitante sem conta), com `generateMetadata` pra SEO e `notFound()` quando o perfil não existe. Testado no browser de ponta a ponta pela primeira vez nesta sessão pra uma página autenticada-opcional: renderiza sem redirecionar pro login (confirma que é mesmo pública) e degrada graciosamente pro "serviço indisponível" sem erro de console. Escopo não cobre uma tela de resultado de busca linkando pra cá — ver P-038. `tsc`/`next build`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E2-03-paginas-publicas` em `main`** (fast-forward, sem conflito). Com isso, o épico E2 (Marketplace e Busca) está com as 6 tasks P0/P1 do backlog completas |
| 2026-07-29 | Cronograma de etapas (E6-01, parcial): `MilestonesService`/`MilestonesController` novos em `ContractsModule` — `POST/GET /contracts/:contractId/milestones` e `PATCH .../:id/{iniciar,entregar,aprovar}`. Máquina de estados casa exatamente com o loop central do `CLAUDE.md` ("prestador executa a etapa e entrega evidências [fotos+checklist] → cliente aprova"): `CONTRATANTE` cria etapas e aprova entregas; `CONTRATADO` inicia execução e entrega (fotos). O model `Milestone` já existia desde S0-05 (nunca tinha endpoints). Sem Gantt visual nem dependências entre etapas — ver P-039. **Resolvido P-036**: `ReviewsService.create()` agora exige pelo menos 1 milestone `APROVADO` no contrato antes de aceitar uma avaliação, exatamente como a pendência antecipava. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E6-01-milestones` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Varredura de pendências resolvíveis (a pedido): triados todos os itens abertos de `PENDENCIAS.md` entre "resolvível agora sem decisão externa" e "precisa de Fundador/Tech Lead/fornecedor/contratação". Resolvidos nesta sessão: **P-005** (pasta `app/` legada estava vazia — removida), **P-013** (confirmado por pesquisa que `pgvector/pgvector:pg16` não tem PostGIS — `infra/docker/Dockerfile.postgres` própria a partir de `postgis/postgis:16-3.5` + pgvector compilado, `docker-compose.local.yml` atualizado; não testado com Docker real), **P-023** (`WorksService`/`ProfileService` — escrita de `geo` agora atômica com o create/upsert via `$transaction`), **P-028** (`RfqProposalService.submit()` agora usa `pg_advisory_xact_lock` por prestador dentro de uma transação, fechando a race do teto mensal), **P-032** (`RfqService.getMine()` agora também permite o prestador que propôs, não só o cliente dono), **P-034** (`GET /search/*` agora é público — a condição que a própria pendência citava, E2-03 existir, já foi cumprida). **Não resolvíveis agora** (decisão de Fundador/Tech Lead, fornecedor externo ou orçamento, fora do meu alcance): P-001 (stack final), P-002 (PSP/escrow), P-003 (cidade-piloto), P-006 (assinatura eletrônica/KYC), P-008 (secrets de CI/CD — precisa da infra decidida), P-011 (DSN real do Sentry), P-012 (contratar engenheiro civil), P-031 (testar DEPLOY.md numa VPS real), P-033 (revisão das calculadoras pelo engenheiro), P-037 (critério de selo). `tsc`/`nest build`/`next build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `fix/pendencias-resolviveis` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Diário de obra (E6-03, parcial): `AuditLog` ganhou `obraId` opcional (migração manual, `onDelete: SetNull` — não conflita com a trigger append-only, que só bloqueia `UPDATE`/`DELETE`, não `ALTER TABLE`). `WorksService`, `RfqService`, `RfqProposalService`, `ContractsService`, `MilestonesService` e `ReviewsService` agora informam `obraId` em cada `auditLog.record()` (para milestones/reviews, que não têm `obraId` direto, um pequeno `getObraId()` busca via `Contract.obraId`). Novo `GET /works/:id/diario` (`WorksService.listDiario`, restrito ao cliente dono) devolve o feed cronológico. Reaproveitou o `audit_log` já existente desde S0-06 em vez de criar uma tabela de eventos dedicada. Sem UI em `apps/web` e sem fotos geolocalizadas (não existe em nenhum upload do sistema) — ver P-040. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E6-03-diario-obra` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Painel financeiro da obra (E6-02, parcial): `GET /works/:id/financeiro` (`WorksService.getPainelFinanceiro`, restrito ao cliente dono) — compara `orcamentoPrevistoCentavos` da obra com a soma de `Milestone.valorCentavos` por etapa (`valorPrevistoCentavos`) e o valor já aprovado (`valorAprovadoCentavos`, só etapas `APROVADO`/`PAGO`), buscando via `contract.obraId` (uma obra pode ter várias RFQs → vários contratos). Matemática validada com dados de exemplo fora do framework (`node -e`). Nomenclatura deliberada: "aprovado", não "realizado" — ver P-041 pelo porquê. Sem UI em `apps/web`. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E6-02-painel-financeiro` em `main`** (fast-forward, sem conflito) |
| 2026-07-29 | Gestão de equipe da obra (E6-04, parcial): novo model `WorkTeamMember` (migração manual, `@@unique([obraId, userId])`) e `WorkTeamController`/`WorkTeamService` em `WorksModule` — `POST/GET/DELETE /works/:id/equipe/[:userId]`. Modelo de permissão decidido com o usuário via checklist (`AskUserQuestion`): **visualização compartilhada, só leitura** — membro nunca ganha poder de escrita, tudo continua exclusivo do cliente dono. `WorksService` ganhou `assertVisible()` (dono OU membro), usado por `getMine`/`listDiario`/`getPainelFinanceiro` (a versão estrita `getOwnedOrThrow` continua reservada pra `update()` e pras próprias mutações de equipe); `MilestonesService.listForContract()` ganhou o mesmo tratamento (`requirePartyOrTeamMember`, via `Contract.obraId`). **E6-05 (controle de entregas de materiais) foi pulado por decisão do usuário** — depende do épico E7 (compras/logística), ainda não iniciado; não registrado como pendência por não ser um item resolvível, e sim um adiamento intencional até E7 começar. Sem UI em `apps/web` — ver P-042. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | Lista de materiais (E7-01, parcial): novo `ProcurementModule` (`services/api/src/modules/procurement/`, nome já previsto em `CLAUDE.md` §4) — `MaterialListsService`/`MaterialListsController`, `POST/GET/GET:id/PATCH /material-lists`. Os models `MaterialList`/`PurchaseQuote`/`MaterialListOrigin` já existiam no schema desde S0-05, sem endpoints. Escrita exclusiva do cliente dono da obra; leitura reaproveita `WorksService.assertVisible()` (E6-04, dono OU membro da equipe) — por isso `WorksModule` passou a `export: [WorksService]`. Só `origem: MANUAL` implementado — ver P-043 pelo porquê de IA/cotação/comparador/checkout ficarem de fora. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-29 | **Merge de `feat/E6-04-equipe` e `feat/E7-01-lista-materiais` em `main`** (fast-forward em sequência, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E6 (Planejamento e Gestão da Obra) completo (E6-01 a E6-04, E6-05 pulado por decisão do usuário) e épico E7 (Compras Inteligentes) iniciado (E7-01 de 4 tasks) |
| 2026-07-29 | Cotação automática multi-fornecedor (E7-02, parcial): `PurchaseQuotesService`/`PurchaseQuotesController` novos em `ProcurementModule` — `POST /material-lists/:id/quote` (cliente dono, casa com até 10 fornecedores por `ProfileFornecedor.categorias`, sem duplicar cotação já existente pro mesmo fornecedor), `GET /material-lists/:id/quotes` (dono ou equipe, via `WorksService.assertVisible`), `GET /purchase-quotes` (fornecedor, próprias) e `PATCH /purchase-quotes/:id` (fornecedor responde: preço por item + frete + prazo, status `RESPONDIDA`). Item de lista ganhou `categoria` opcional (`packages/types/src/material-lists.ts`, sem migração — `itens` é Json) só pra alimentar esse matching. Sem geo/raio nem rodízio — ver P-044. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | Comparador de cotações (E7-03, parcial): `PurchaseQuotesService.getComparison()` + `GET /material-lists/:id/comparison` (dono ou equipe) — considera só cotações `RESPONDIDA`, calcula `totalCentavos` (itens arredondados pra inteiro + frete) e marca 4 destaques booleanos (`menorPreco`/`menorFrete`/`melhorAvaliacao`/`menorPrazo`), permitindo empates múltiplos. Lógica de ranking validada fora do framework (`node -e` com dados sintéticos, 3 fornecedores). Sem pontuação combinada nem UI — ver P-045. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E7-03-comparador` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E7 com E7-01/E7-02/E7-03 completos, só falta E7-04 (checkout, bloqueado por P-002/PSP) |
| 2026-07-30 | Checkout de compra com **PSP simulado** (E7-04, parcial, a pedido explícito do usuário pra não esperar P-002): novo model `PurchaseOrder` (1:1 com `PurchaseQuote` via `@unique`, idempotente) + `PurchaseQuotesService.checkout()` / `POST /purchase-quotes/:id/checkout` — calcula `itensTotalCentavos` + `freteCentavos` + comissão (`PROCUREMENT_COMMISSION_BPS`, placeholder 5% dentro da faixa 3–8% do backlog) e cria a ordem com `pspRef` sintético (`SIMULADO-<uuid>`), sempre sucesso, sem chamar nenhum provedor real. Matemática de centavos (inclusive quantidade fracionária) validada fora do framework (`node -e`). **Nenhum dinheiro real se move** — ver P-046 pro detalhamento completo do que isso não cobre (falha/estorno, NFS-e, extrato, UI). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E7-04-checkout-simulado` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E7 (Compras Inteligentes) completo: E7-01/E7-02/E7-03/E7-04. E7-04 usa PSP simulado (P-046) até P-002 ser resolvido |
| 2026-07-30 | Hub Financeiro/Escrow com **PSP simulado** (E4, núcleo do produto, parcial, a pedido explícito do usuário pra não travar o resto do desenvolvimento esperando P-002): novo `EscrowModule` (`services/api/src/modules/escrow/`, nome já previsto em `CLAUDE.md` §4) reaproveitando `EscrowTransaction`/`Dispute` (schema + trigger append-only já existiam desde S0-05, nunca tinham endpoints) — `POST /contracts/:id/milestones/:id/deposito` (CONTRATANTE deposita o valor da etapa `PENDENTE`, idempotente) e `GET /contracts/:id/escrow` (extrato, parte do contrato ou equipe). `MilestonesService.aprovar()` agora chama `EscrowService.liberarSeDepositado()` automaticamente: se houver depósito confirmado, cria `LIBERACAO` (líquido) + `COMISSAO` (retida, `ESCROW_COMMISSION_BPS`, placeholder 3% dentro da faixa 2–5% do PRD) e avança `Milestone.status` pra `PAGO`; sem depósito prévio, comportamento inalterado (`APROVADO`, decisão de compatibilidade confirmada via checklist). Ledger com hash encadeado (SHA-256 + `previousHash`, `pg_advisory_xact_lock` serializando concorrência — implementa E4-05) validado fora do framework (`node -e`: cadeia íntegra, líquido+comissão sempre bate com o depósito). **Corrigido**: `ReviewsService.create()` só aceitava milestone `APROVADO` no gate de avaliação — com etapas agora podendo chegar a `PAGO`, o gate passou a aceitar os dois estados (senão contratos com etapas liberadas parariam de contar, o oposto do esperado). Fora do escopo desta rodada: contrato PDF/assinatura eletrônica (E4-01/02, dependem de P-006), timeout de aprovação automática (E4-08), disputa/estorno (E4-09/10, `Dispute` sem endpoints ainda), webhook (E4-11, não se aplica a PSP simulado), NFS-e (E4-13) e UI em `apps/web` — ver P-047 pro detalhamento completo. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E4-escrow-simulado` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — núcleo do Hub Financeiro/Escrow (E4) rodando com PSP simulado, ver P-047 |
| 2026-07-30 | Fluxo de disputa (E4-09/E4-10, parcial): novos `DisputesService`/`DisputesController` em `EscrowModule`, reaproveitando `Dispute` (schema desde S0-05, sem endpoints). `POST /contracts/:id/milestones/:id/disputas` (qualquer parte do contrato abre, com motivo + evidências) marca `Milestone.status = EM_DISPUTA` — congela iniciar/entregar/aprovar sozinho, sem precisar de guarda extra, porque nenhum dos status checks existentes casa com `EM_DISPUTA` (fecha exatamente o requisito "disputa congela liberação imediatamente" do checklist B.3). `GET .../disputas` (parte/equipe) e `GET /disputas` (fila de mediação, ADMIN). `PATCH /disputas/:id/resolver` (ADMIN): `APROVAR` reverte pra `ENTREGUE` (segue o fluxo normal); `ESTORNAR`/`LIBERAR_PARCIAL` chamam os 2 métodos novos de `EscrowService` (`estornarDeposito`/`liberarParcial`) que criam `ESTORNO`/`LIBERACAO` parcial no mesmo ledger de hash encadeado do escrow — validados fora do framework (`node -e`): líquido + comissão + estornado sempre bate com o valor depositado, em 3 cenários (parcial, estorno total, liberação total). Sem SLA de mediação, sem notificação e sem calculadora de proporção pro mediador — ver P-048 (inclui a observação de que nenhum usuário `ADMIN` existe ainda no sistema pra de fato mediar). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E4-disputas` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E4 (Hub Financeiro/Escrow) com núcleo + disputa/estorno rodando em PSP simulado, ver P-047/P-048 |
| 2026-07-30 | Seed de desenvolvimento (`prisma/seed.ts`, a pedido do usuário) estendido pra cobrir as 5 personas de teste ponta a ponta: **admin** (`admin@conectaobra.example.com`, mediador de disputas — resolve parcialmente o "nenhum ADMIN existe" de P-048), cliente e prestador (já existiam), **engenheiro** (`julia.engenheira@example.com`, `tipo: TECNICO` reaproveitando `ProfilePrestador` conforme decisão de E1-02/P-015, categorias `estrutural`/`projetos`) e fornecedor (já existia). Senha de todos: `senha12345`. `tsc`/`pnpm build`/`lint`/`test` da raiz passam. **Não executado contra um Postgres real** — sem Docker disponível nesta sessão (mesma limitação de sempre, P-010); rodar com `pnpm --filter @conectaobra/api seed` assim que houver banco |
| 2026-07-30 | **Merge de `chore/seed-personas-teste` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) |
| 2026-07-30 | `git pull` trouxe commit de outra sessão/máquina (`e5d4012`, com Docker disponível de verdade): corrige hot-reload do `docker-compose.dev.yml` (procps ausente derrubava o container do `nest start --watch`; healthcheck do container `packages` esperando marcador pós-build), remove resíduo de debug (`"ok-hotreload-test"`) do `health.controller.ts`, e cria `docs/prd/07_Personas_Seed_Dev.md` documentando as 5 personas do seed com exemplo de `curl` de login |
| 2026-07-30 | Aprovação automática por timeout (E4-08, parcial): `Milestone.entregueEm` (nova migração) + `MilestoneTimeoutService`/`MilestoneTimeoutProcessor` (fila BullMQ `milestone-timeout` em `ContractsModule`) — `entregar()` agenda avisos D-3/D-1 (só logam) e auto-aprovação em D-0 (`MILESTONE_AUTO_APROVACAO_DIAS`, default 7 dias, confirmado com o usuário via checklist). Cada job relê o status atual da etapa antes de agir — se já saiu de `ENTREGUE` (aprovada manualmente ou disputada), é um no-op, sem precisar cancelar jobs pendentes. `MilestonesService.aprovar()` refatorado pra compartilhar `finalizarAprovacao()` com o novo `aprovarAutomaticamente()`, evitando duplicar a lógica de liberação de escrow. Lógica de agendamento (guardas D-3/D-1 pra prazos curtos, atalho `dias=0` pra testar em dev) validada fora do framework (`node -e`). Sem endpoint de consulta e sem mecanismo de reconciliação contra perda de job — ver P-049. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E4-08-timeout-aprovacao` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E4 com núcleo + disputa/estorno + timeout de aprovação rodando em PSP simulado, só faltam E4-01/02 (contrato/assinatura, P-006), E4-11 (webhook, N/A) e E4-13 (NFS-e) |
| 2026-07-30 | Extrato financeiro do prestador/fornecedor (E4-12, parcial): `EscrowService.getExtrato()` + `GET /extrato-financeiro` (novo `ExtratoFinanceiroController` em `EscrowModule`) — soma todas as transações `LIBERACAO` dos contratos em que o usuário é `CONTRATADO`, com a descrição da etapa e `pspRef` como comprovante, mais o total recebido. Vazio pra quem nunca foi contratado de nada (cliente puro), sem exigir tipo de usuário específico. Agregação validada fora do framework (`node -e`). Sem paginação/filtro por período e sem PDF de comprovante — ver P-050. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E4-12-extrato-financeiro` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E4 com todas as tasks P0 desbloqueadas concluídas; só restam E4-01/02 (assinatura, P-006), E4-11 (webhook, N/A) e E4-13 (NFS-e, externo) |
| 2026-07-30 | Pipeline RAG com embeddings **SIMULADOS** (E5-01, parcial, a pedido explícito do usuário — mesmo espírito do PSP simulado, pra não travar esperando a escolha de um provedor real): novo model `KnowledgeChunk` (coluna `vector(256)` via `Unsupported`, mesmo padrão de `geography`) + `KnowledgeService`/`KnowledgeController` em `AiModule` — `POST /ai/knowledge` (ingestão, exclusiva do ADMIN: chunking por parágrafo + embedding + insert via `$queryRaw`) e `GET /ai/knowledge/search` (retrieval por similaridade de cosseno, operador `<=>` do pgvector). `embedText()` é feature hashing determinístico (bag-of-words) — documentado como simulação sem qualidade semântica real; trocar por um provedor real (OpenAI/Voyage AI/Cohere) só exige reescrever essa função. Chunking (parágrafo maior que o limite vira chunk próprio) e embedding (determinismo, norma unitária, similaridade maior entre textos com palavras em comum) validados fora do framework (`node -e`). Sem índice vetorial, sem validação de conteúdo na ingestão e sem UI — ver P-051. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E5-01-pipeline-rag` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E5 iniciado: E5-04 (calculadoras) já existia, E5-01 (pipeline RAG, embeddings simulados) concluído agora |
| 2026-07-30 | Chat do "Engenheiro Virtual" com resposta **SIMULADA** (E5-03, parcial, a pedido explícito do usuário — mesmo espírito do PSP/embeddings simulados, pra não travar esperando a integração real da Claude API): novo `AiChatService`/`ChatController` em `AiModule` — `POST /ai/chat` (busca no RAG via `KnowledgeService.search()`, monta resposta-template citando as fontes recuperadas, aplica disclaimer via `risk-classifier.util.ts` — versão simples por palavra-chave pro tema estrutural/elétrico/gás, CLAUDE.md §5 regra 3 — e persiste em `AiConversation`, criando ou continuando via `conversaId`), `GET /ai/chat` (lista minhas conversas) e `GET /ai/chat/:id`. Reaproveita `AiConversation` (schema desde S0-05, sem endpoints até agora). Classificador de risco testado isoladamente fora do framework (`node -e`): dispara certo pra pergunta estrutural/elétrica, não dispara pra pergunta neutra, e dispara mesmo com pergunta neutra se a fonte recuperada tiver categoria de risco. Sem streaming real, sem contagem real de tokens (estimativa por caracteres), sem rate-limit/consentimento de IA dedicado — ver P-052 (guard-rails completos ficam pro E5-06). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E5-03-chat-simulado` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E5 com E5-01/E5-03/E5-04 rodando (embeddings e chat simulados); restam E5-02 (curadoria, bloqueada por P-012), E5-05 (analisador de orçamento), E5-06 (guard-rails completos) e E5-07 (gerador de lista via IA) |
| 2026-07-30 | Analisador de orçamento (E5-05, parcial) e gerador de lista de materiais via IA (E5-07, parcial), ambos em `AiModule`: `BudgetAnalyzerService`/`Controller` reaproveitam `AvgCost` (schema desde S0-05, sem endpoints) — `POST /ai/avg-costs` (ADMIN, upsert manual de custo médio) e `POST /ai/analisar-orcamento` (classifica `ABAIXO`/`DENTRO`/`ACIMA_DA_MEDIA` pela faixa min/max, calcula desvio percentual — comparação puramente determinística). `MaterialGeneratorService` (novo, exportado do `AiModule`) gera itens por regra de palavra-chave sobre a descrição da obra, reaproveitando `CalculatorsService` (E5-04) pra quantidades precisas de tinta/blocos/argamassa quando a área é informada; itens sem fórmula (elétrica/hidráulica) viram "verba" com observação pedindo revisão manual, nunca fabricando precisão falsa. `ProcurementModule` passou a importar `AiModule`; `MaterialListsService.gerarComIA()` + `POST /material-lists/gerar-ia` criam a lista já com `origem: IA` (409 se nenhuma regra bateu). Classificação de orçamento (3 cenários) e regras do gerador (com/sem área, descrição vaga) validadas fora do framework (`node -e`). Sem dado real de custo médio nem UI — ver P-053. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E5-05-e-E5-07` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E5 com E5-01/E5-03/E5-04/E5-05/E5-07 rodando (simulados); restam E5-02 (curadoria, bloqueada por P-012) e E5-06 (guard-rails completos) |
| 2026-07-30 | Guard-rails do chat de IA (E5-06, parcial): `AI_CHAT_FREE_PLAN_DAILY_LIMIT` (default 10/dia, mesmo padrão de `FREE_PLAN_MONTHLY_PROPOSAL_LIMIT`/P-025) + `AiChatService.enforceDailyLimit()` — conta mensagens de hoje pelo `createdAt` de cada mensagem dentro do Json (sem `Subscription` = plano gratuito = limite; qualquer `Subscription` = sem limite). Log de auditoria (`ai_chat.mensagem`) em toda troca, com `disclaimerAplicado` no payload — `AiModule` passou a importar `AuditLogModule`. Classificador de risco (`risk-classifier.util.ts`) ganhou mais termos (rachadura/trinca/fissura/recalque/escoramento, curto-circuito/choque elétrico, cheiro de gás). Contagem diária e keywords novas validadas fora do framework (`node -e`): filtra corretamente mensagens de outros dias, bloqueia exatamente no limite, e as novas palavras disparam disclaimer sem falso positivo em pergunta neutra. Ainda é lista de palavras-chave, não um classificador de intenção real — ver P-054. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | **Merge de `feat/E5-06-guard-rails` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E5 completo exceto E5-02 (curadoria da base, bloqueada por P-012 — contratação do engenheiro consultor, ação humana fora do escopo de código) |
| 2026-07-30 | Lojas/filiais do fornecedor (extensão de E1-06, parcial — 1ª de 3 rodadas pedidas pelo usuário: lojas → promoções → produtos+Excel): novo model `FornecedorLoja` (fornecedor pode ter várias lojas, cada uma com endereço/região/telefone/geo próprios, mesmo padrão de `PortfolioItem`) + `LojasService`/`Controller` em `ProfileModule` — `POST/GET/PATCH/DELETE /profile/fornecedor/lojas[/:id]`, restrito ao FORNECEDOR dono. Geo gravado via `$executeRaw` numa transação com o create/update (mesmo padrão de `WorksService.setGeo`, P-023). Sem geocoding automático e sem vínculo produto/cotação→loja específica — ver P-055. Backend apenas, sem tela em `apps/web` ainda (confirmado com o usuário: backend primeiro, telas depois). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-30 | Merge de `feat/fornecedor-lojas` em `main` — lojas/filiais do fornecedor (1ª de 3 rodadas: lojas → promoções → produtos+Excel). Revalidado em `main`: `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes |
| 2026-07-31 | Promoções do fornecedor (extensão de E1-06, parcial — 2ª de 3 rodadas: lojas → promoções → produtos+Excel): novo model `Promocao` (código, nome, descrição, valor original/promocional em centavos, imagem, validade início/fim, destaque, ativa; `@@unique([fornecedorId, codigo])`) + `PromocoesService`/`Controller` em `ProfileModule` (`POST/GET/PATCH/DELETE /profile/fornecedor/promocoes[/:id]`, restrito ao FORNECEDOR dono, mesmo padrão de `LojasService`). Novo `PromotionsModule` com `PublicPromocoesController` (`GET /public/promocoes`, sem guard — primeiro endpoint público desde `PublicProfilesModule`/E2-03) filtra só promoções `ativa=true` e dentro da validade, com `?destaque=true` pros destaques da home. Validações zod (validade futura, início antes do fim) e coerção de query booleano validadas fora do framework (`node -e`). Imagem é só uma URL (`imagemUrl`) — o front sobe o arquivo via `POST /media/presigned-upload` (E1-07) e manda a `publicUrl` de volta. Sem moderação de conteúdo, sem contagem de uso do código, sem limite de destaques por fornecedor e sem UI em `apps/web` — ver P-056. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-07-31 | Merge de `feat/fornecedor-promocoes` em `main` — promoções do fornecedor (2ª de 3 rodadas: lojas → promoções → produtos+Excel). Revalidado em `main`: `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes |
| 2026-08-01 | Produtos com código + import Excel (extensão de E1-06/E2-06, 3ª e última rodada do pedido do usuário: lojas → promoções → produtos+Excel): `Product` ganhou `codigo` (`@@unique([fornecedorId, codigo])`, múltiplos `NULL` permitidos pra não quebrar produtos já existentes) e `descricao` (`@db.Text`). `ProductsService.create()`/`update()` passaram a tratar violação de unicidade do código como `ConflictException` (409, mesmo padrão de `WorkTeamMember`/P-056). Novo `xlsx-import.util.ts` (`parseProductsSpreadsheet`, `parseValorParaCentavos`) lê a 1ª aba de uma planilha via `xlsx`/SheetJS, tolera acento/maiúsculas/sinônimos no cabeçalho (`HEADER_ALIASES`) e vírgula decimal pt-BR ("R$ 1.234,56"), validado fora do framework (`tsx` standalone: cabeçalho correto, código duplicado/descrição em branco/valor inválido, cabeçalho ausente). `ProductsService.importFromExcel()` faz upsert por `(fornecedorId, codigo)` linha a linha (não é transação única — uma falha isolada não derruba o resto), categoria default `"Geral"` pra linhas importadas (planilha não tem coluna de categoria), reindexa cada produto no Meilisearch e registra um `audit_log` resumido. `POST /products/import` (multipart, campo `file`, FORNECEDOR-only, limite 5MB, só aceita `.xlsx`/`.xls`) usa `FileInterceptor` do `@nestjs/platform-express` — precisou adicionar `"multer"` no `types` do `tsconfig.json` (senão `Express.Multer.File` não resolve, já que o projeto restringe `types` só a `["node"]`). Sem UI em `apps/web` e sem coluna de categoria na planilha — ver P-057. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-08-01 | Merge de `feat/produtos-import-excel` em `main` — produtos com código + import Excel (3ª e última rodada: lojas → promoções → produtos+Excel). Revalidado em `main`: `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes. **As 3 rodadas do pedido do usuário estão completas no backend** — próximo passo (não iniciado): telas em `apps/web` (painel do fornecedor pra lojas/promoções/produtos+import, página pública de promoções, destaques na home, e a tela de cadastro/login/dashboard do prestador de serviço, adiada desde a Round 1) |
| 2026-08-01 | Painel do fornecedor em `apps/web` (1ª rodada de telas do frontend, começando pelas 3 rodadas de backend recém-concluídas): nova rota `/conta/fornecedor` (Server Component, protegida pelo `middleware.ts` já existente via matcher `/conta/:path*`) com abas (`Tabs` do `packages/ui`, primeiro uso real desse componente no app) — **Perfil** (form `react-hook-form` + `fornecedorProfileInputSchema`, campos de lista como texto separado por vírgula) e **Lojas** (list/create/edit/delete inline, sem modal) funcionais; **Promoções**/**Produtos** só com texto de placeholder apontando pra API já disponível (próximas rodadas). Novos Route Handlers de proxy (`app/api/profile/fornecedor`, `app/api/fornecedor/lojas[/[id]]`) seguem o padrão já usado em `app/api/proposals/[id]/accept` — leem o cookie httpOnly `co_access_token`, encaminham pro NestJS, tratam `ApiUnavailableError`. Link de `/conta` pra `/conta/fornecedor` adicionado condicional a `tipo === "FORNECEDOR"`. Sem Docker/Postgres neste ambiente (mesma limitação de sempre): validado até onde dá — `tsc`/`next build` passam, middleware barra acesso sem sessão, e a página trata indisponibilidade da API corretamente (testado com cookie de sessão falso) — mas o fluxo real de CRUD nunca rodou contra a API de verdade. Ver P-059 |
| 2026-08-01 | Merge de `feat/web-fornecedor-dashboard` em `main` — painel do fornecedor, abas Perfil e Lojas (1ª rodada de telas do frontend). Revalidado em `main`: `tsc`/`next build`, `pnpm build`/`lint`/`test` da raiz — todos verdes. Próximas rodadas (não iniciadas): aba Promoções + página pública + destaques da home; aba Produtos + import de planilha; painel do prestador (Perfil + Portfólio) |
| 2026-08-01 | Telas de promoções em `apps/web` (2ª rodada do frontend): aba Promoções no painel do fornecedor (`promocoes-panel.tsx`, list/create/edit/delete inline, campos código/nome/descrição/valores em R$/imagem/validade/destaque/ativa — checkboxes via `Controller` do react-hook-form pra integrar com o `Checkbox` do Radix). Novos Route Handlers de proxy (`app/api/fornecedor/promocoes[/[id]]`) mesmo padrão de lojas. Página pública `/promocoes` (Server Component, sem auth, chama `GET /public/promocoes` direto) e seção "Promoções em destaque" na home (`GET /public/promocoes?destaque=true&limit=3`, "fail open" — se a API cair a seção só some, sem alerta, pra nunca quebrar a landing page). Sem Docker/Postgres neste ambiente: validado até onde dá (`tsc`/`next build`, tratamento de indisponibilidade em `/promocoes` e na home, aba do painel carrega sem erro de console) — CRUD real nunca rodou contra dados de verdade. Ver P-060 |
| 2026-08-01 | Merge de `feat/web-promocoes` em `main` — telas de promoções (2ª rodada de telas do frontend: lojas → promoções → produtos+Excel). Revalidado em `main`: `tsc`/`next build`, `pnpm build`/`lint`/`test` da raiz — todos verdes. Próximas rodadas (não iniciadas): aba Produtos + import de planilha; painel do prestador (Perfil + Portfólio) |
| 2026-08-01 | Aba Produtos no painel do fornecedor (3ª e última rodada de telas do frontend: lojas → promoções → produtos+Excel): `produtos-panel.tsx` com CRUD (nome/categoria/preço em R$/unidade/estoque/código/descrição) + formulário de import de planilha (`ImportarPlanilha`, input `type="file"` .xlsx/.xls, mostra criados/atualizados/erros por linha do `ImportProductsResult`). Novos Route Handlers `app/api/products[/[id]]` (mesmo padrão de lojas/promoções) e `app/api/products/import` — este último não pode usar `apiFetchOrThrow` (força `content-type: application/json`, quebraria o boundary multipart), então recebe o upload via `req.formData()`, reempacota num novo `FormData` e chama `fetch` direto pro `services/api`. Após import bem-sucedido, a tela recarrega a página inteira (upsert no banco não devolve a lista completa). Sem Docker/Postgres neste ambiente: validado até onde dá (`tsc`/`next build`, aba carrega sem erro de console) — CRUD e import reais nunca rodaram contra a API de verdade. Ver P-061. **Com isso, as 3 rodadas de telas do frontend do pedido original do usuário estão completas** — resta o painel do prestador (Perfil + Portfólio), adiado desde a Round 1 |
| 2026-08-01 | Merge de `feat/web-produtos` em `main` — aba Produtos + import de planilha (3ª e última rodada de telas do frontend: lojas → promoções → produtos+Excel). Revalidado em `main`: `tsc`/`next build`, `pnpm build`/`lint`/`test` da raiz — todos verdes. **As 3 rodadas de telas do pedido original do usuário estão completas.** Próxima rodada (não iniciada): painel do prestador (Perfil + Portfólio), adiado desde a Round 1 do backend |
| 2026-08-01 | Painel do prestador em `apps/web` (`/conta/prestador`): abas Perfil (categorias/experiência/certificados/raio de atendimento, texto separado por vírgula igual ao perfil do fornecedor) e Portfólio (CRUD de itens — título/descrição — mesmo padrão de lojas/promoções/produtos). Novos Route Handlers `app/api/profile/prestador` (PUT) e `app/api/prestador/portfolio[/[id]]` (POST/PATCH/DELETE). Link condicional `/conta` → `/conta/prestador` quando `tipo` é `PRESTADOR` ou `TECNICO`. `/conta/prestador` já cai sob o matcher `/conta/:path*` do `middleware.ts` — nenhuma mudança de middleware necessária. Sem Docker/Postgres neste ambiente: validado até onde dá (`tsc`/`next build`, middleware confirmado bloqueando sem sessão, tratamento de indisponibilidade da API) — CRUD real nunca rodou contra dados de verdade. Ver P-062. **Com isso, as 4 rodadas de telas do frontend pedidas nesta sessão (fornecedor: lojas/promoções/produtos + prestador) estão completas** |
| 2026-08-01 | Merge de `feat/web-prestador` em `main` — painel do prestador, abas Perfil e Portfólio (última rodada de telas do frontend). Revalidado em `main`: `tsc`/`next build`, `pnpm build`/`lint`/`test` da raiz — todos verdes. **Todo o pedido original do usuário (backend + frontend de lojas/promoções/produtos+Excel do fornecedor, mais a tela do prestador) está completo** |
| 2026-08-02 | Backend de anúncios de fornecedor/prestador (E9-06, escopo reduzido — confirmado com o usuário via `AskUserQuestion`: CRUD completo, sem cobrança/billing real nesta rodada). `Ad` (existia desde S0-05) ganhou `ativo Boolean` pra controlar visibilidade. Novo módulo `AdsModule`: `AdsService`/`Controller` (`POST/GET/PATCH/DELETE /ads`, restrito a FORNECEDOR/PRESTADOR/TECNICO) + `PublicAdsService`/`Controller` (`GET /public/ads`, sem login, pra exibição na home) — mesmo padrão de `PromocoesService`/`PublicPromocoesController`. `criativo` (Json livre) ganhou uma forma validada (`adCriativoSchema`: título/descrição/imagemUrl/linkUrl). Sem moderação de conteúdo, sem leilão/ranking de anúncios, sem contagem de clique/impressão nem desconto de budget (`metricas` nunca escrito) — ver P-063. Validações zod checadas fora do framework (`node -e`). `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-08-02 | Merge de `feat/ads-backend` em `main` — backend de anúncios de fornecedor/prestador (E9-06, escopo reduzido). Revalidado em `main`: `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada (não iniciada): aba Anúncios nos painéis de fornecedor e prestador + seção de anúncios na home |
| 2026-08-02 | Telas de anúncios em `apps/web`: componente compartilhado `AdsPanel` (novo, em `components/`) usado como aba "Anúncios" em ambos os painéis (fornecedor e prestador) — CRUD completo (tipo/criativo/budget informativo/ativo), novos Route Handlers de proxy `app/api/ads[/[id]]` (únicos, não duplicados por tipo de usuário, já que o backend é genérico). Seção "Anúncios de fornecedores e prestadores" na home (`GET /public/ads?limit=3`, fail open) — cada card linka pro `linkUrl` do anúncio (nova aba) ou, sem link configurado, pro perfil público do anunciante (`/fornecedores/:id`/`/prestadores/:id`). Sem Docker/Postgres neste ambiente: validado até onde dá (`tsc`/`next build`, home e as duas abas carregam sem erro de console, tratamento de indisponibilidade da API) — CRUD real nunca rodou contra dados de verdade. Ver P-064 |
| 2026-08-02 | Merge de `feat/web-ads` em `main` — telas de anúncios de fornecedor/prestador na home (E9-06, escopo reduzido). Revalidado em `main`: `tsc`/`next build`, `pnpm build`/`lint`/`test` da raiz — todos verdes |
| 2026-08-03 | Backend de notícias + biblioteca (E9-01/E9-04, 1ª de várias rodadas do épico E9 — confirmado com o usuário via `AskUserQuestion`: indicadores por cadastro manual ADMIN, marca d'água implementada de verdade nas próximas rodadas do catálogo de plantas). `Article` (existia desde S0-05, zero endpoints) ganhou `arquivoUrl` opcional. Novo módulo `ContentModule`: `ArticlesService`/`Controller` (`POST/GET/PATCH/DELETE /articles`, ADMIN-only, mesmo padrão de `KnowledgeChunk`/`AvgCost`) com slug auto-gerado do título (`slugify.util.ts`, tolerante a acento, desambiguado com sufixo -2/-3 em colisão — validado fora do framework via `tsx` standalone) ou explícito (unicidade exigida, sem desambiguação silenciosa). `PublicArticlesService`/`Controller` (`GET /public/articles?categoria=&limit=`, `GET /public/articles/:slug`, sem login) só devolve artigos com `publicadoEm` preenchido e no passado. Notícias e biblioteca compartilham o mesmo model/endpoints, diferenciados só por `categoria` (convenção, não enum) — ver P-065. Sem UI em `apps/web` ainda. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-08-03 | Merge de `feat/content-articles` em `main` — notícias + biblioteca (E9-01/E9-04, 1ª rodada do épico E9). Revalidado em `main`: `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: indicadores (E9-02) + tabela de custos por cidade (E9-03) |
| 2026-08-03 | Indicadores + tabela de custos por cidade (E9-02/E9-03, 2ª rodada do épico E9, sem schema novo — `Indicator`/`AvgCost` já existiam desde S0-05). `IndicatorsService`/`Controller` (`POST /indicators`, ADMIN, upsert por tipo+região+mês, mesmo padrão de `upsertAvgCost`/E5-05 — confirmado com o usuário: cadastro manual, sem ingestão automática de fonte externa) + `PublicIndicatorsController`/`PublicAvgCostsController` (`GET /public/indicators`, `GET /public/avg-costs`, sem login). Validações zod checadas fora do framework (`node -e`). Sem UI em `apps/web`, sem seed de dados reais (tabelas ficam vazias até o ADMIN cadastrar) — ver P-066. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-08-03 | Merge de `feat/content-indicators` em `main` — indicadores + tabela de custos por cidade (E9-02/E9-03, 2ª rodada do épico E9). Revalidado em `main`: `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: catálogo de plantas (E9-05) — CRUD + navegação pública |
| 2026-08-03 | Catálogo de plantas — CRUD + vitrine pública (E9-05 parte 1 de 2, 3ª rodada do épico E9). `ProjectCatalog` (existia desde S0-05, zero endpoints) ganhou `descricao`/`imagemCapaUrl`. Novo módulo `ProjectsCatalogModule`: `CatalogProjectsService`/`Controller` (`POST/GET/PATCH/DELETE /catalog/projects`, restrito a PRESTADOR/TECNICO, mesmo padrão de lojas/promoções) + `PublicCatalogProjectsService`/`Controller` (`GET /public/catalog/projects[?categoria=][/:id]`, sem login) — a vitrine pública **nunca** expõe o array `arquivos` (conteúdo pago). Checkout/compra/marca d'água ficam pra parte 2 (confirmado com o usuário: implementar marca d'água de verdade com `sharp`/`pdf-lib`). Sem UI em `apps/web`. `tsc`/`nest build`/`pnpm build`/`pnpm lint`/`pnpm test` da raiz passam |
| 2026-08-03 | Merge de `feat/catalog-projects` em `main` — catálogo de plantas, CRUD + vitrine pública (E9-05 parte 1 de 2, 3ª rodada do épico E9). Branch precisou de rebase (não era mais fast-forward após o merge de `feat/deploy-two-domains`) — conflito só em `PENDENCIAS.md` (changelog, ambas as entradas mantidas em ordem). Revalidado em `main`: `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: catálogo de plantas parte 2 (checkout + comissão + marca d'água real) |
| 2026-08-04 | Catálogo de plantas — checkout + marca d'água real (E9-05 parte 2 de 2, completa o épico E9, 4ª rodada). Novo model `ProjectPurchase` (`@@unique([projectId, compradorId])`, mesma ideia de idempotência de `PurchaseOrder`/E7-04) + `CatalogPurchasesService`/`Controller` (`POST /catalog/projects/:id/buy`, qualquer usuário autenticado; `GET /catalog/purchases`), PSP simulado, comissão via novo `CATALOG_COMMISSION_BPS` (placeholder 20%, faixa "15–25%" do PRD). Novo `WatermarkService` — `sharp`/`pdf-lib` instalados e testados nativamente neste ambiente (`node -e` confirma bindings do `sharp` funcionando) — baixa o arquivo original via `fetch`, sobrepõe marca d'água com o nome do comprador (SVG rotacionado em imagem, `drawText` por página em PDF) e reenvia pro S3; sem S3 configurado ou tipo de arquivo não suportado, entrega o original sem bloquear a compra (`marcaDaguaAplicada: false`). Watermarking roda fora da `$transaction` do Prisma (I/O de rede não pode estourar o timeout de transação interativa). Lógica pura validada fora do framework (`tsx` standalone, sem S3 real): PNG e PDF sintéticos, marca d'água preserva dimensões/contagem de páginas, extensão de arquivo detectada corretamente, fallback sem S3 confirmado. Sem UI em `apps/web`. `tsc`/`nest build`/boot-test (só P1001)/`pnpm build`/`lint`/`test` da raiz passam — ver P-068 |
| 2026-08-04 | Merge de `feat/catalog-checkout` em `main` — checkout + marca d'água real do catálogo de plantas (E9-05 parte 2 de 2, 4ª e última rodada do épico E9 nesta sessão). Revalidado em `main`: `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz — todos verdes. **Épico E9 completo no backend** (notícias/biblioteca, indicadores, custos médios, anúncios, catálogo de plantas com checkout simulado e marca d'água real) — falta UI em `apps/web` pra tudo isso |
| 2026-08-04 | Telas de notícias + biblioteca em `apps/web` (E9-01/E9-04, 1ª rodada de telas do épico E9): `/noticias` (portal), `/noticias/[slug]` (detalhe, com `generateMetadata` pra SEO), `/biblioteca` (mesmo endpoint de artigos, filtrado client-side por `arquivoUrl` presente). Corrigido de quebra: `next dev` estava quebrado nesta sessão (mudança de porta de uma sessão externa deixou `.claude/launch.json` passando `-- -p 3399` malformado pro pnpm, que `next dev` interpretava errado, tentando usar "-p" como diretório do projeto) — corrigido fixando a porta direto no script `dev` de `apps/web/package.json`. Validado no browser: as 3 páginas carregam sem erro de console, tratam indisponibilidade da API corretamente (sem backend neste ambiente). `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam. Sem forma de cadastrar conteúdo pela UI ainda (`apps/admin` é só scaffold vazio) — ver P-069 |
| 2026-08-04 | Merge de `feat/web-content-articles` em `main` — notícias + biblioteca (E9-01/E9-04, 1ª rodada de telas do épico E9). Revalidado em `main`: `tsc`/`next build`, `pnpm build`/`lint`/`test` da raiz — todos verdes, `next dev` funcionando de novo (fix do launch.json). Próxima rodada: indicadores + tabela de custos por cidade (E9-02/E9-03) |
| 2026-08-03 | `DEPLOY.md`/`infra/deploy/nginx.conf.example` atualizados pra dois domínios públicos: `conectaon.unifyhub.com.br` (`apps/web`) e `apiconectaon.unifyhub.com.br` (`services/api`) — dois server blocks no Nginx, um cert Let's Encrypt cobrindo os dois via `certbot -d` duplo. `services/api` deixa de ser só interno (era a arquitetura documentada antes), mas `apps/web` continua chamando a API via `127.0.0.1:3333` internamente — o domínio público da API é pra consumidores externos, sem CORS configurado ainda (não bloqueia nada hoje). `deploy.sh`/`ecosystem.config.cjs` não mudaram (já cobriam "baixar do main e atualizar" sem depender de domínio). Merge direto em `main` (mudança só de documentação/infra, sem código de aplicação) — ver P-031 |
| 2026-07-29 | **Merge de `feat/E7-02-cotacao-automatica` em `main`** (fast-forward, sem conflito). `main` revalidado de ponta a ponta pós-merge (`pnpm build`/`lint`/`test` da raiz) — épico E7 com E7-01 e E7-02 completos, E7-03 (comparador) e E7-04 (checkout, bloqueado por P-002) restantes |
| 2026-08-04 | Telas de indicadores + custos médios em `apps/web` (E9-02/E9-03, 2ª rodada de telas do épico E9): `/indicadores` (filtro GET por tipo/região, sem client JS) e `/custos-medios` (filtro GET por cidade/serviço, tabela com mín/médio/máx formatado em R$) — mesmo padrão de `/noticias` (Server Component, `ApiUnavailableError` tratado). Validado no browser: as 2 páginas carregam sem erro de console, tratam indisponibilidade da API corretamente (sem backend neste ambiente). `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam. Mesma lacuna de P-069: sem UI ADMIN pra cadastrar indicadores/custos, as tabelas ficam vazias em qualquer ambiente novo — ver P-070 |
| 2026-08-04 | Merge de `feat/web-indicators` em `main` (fast-forward) — indicadores + custos médios (E9-02/E9-03, 2ª rodada de telas do épico E9). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: catálogo de plantas — vitrine pública + aba no painel do prestador (E9-05, frontend) |
| 2026-08-04 | Telas de catálogo de plantas em `apps/web` (E9-05, 3ª e última rodada de telas do épico E9 — **completa o épico E9 no frontend**). Vitrine pública `/catalogo` + `/catalogo/[id]` (com `ComprarButton`, redireciona pro login se não houver sessão), aba "Catálogo" no painel do prestador (`CatalogoPanel`, CRUD completo, mesmo padrão de `AdsPanel`), nova página `/compras` (qualquer usuário autenticado, adicionada ao matcher do `middleware.ts`). Novos route handlers `app/api/catalog/projects[/[id]][/buy]`. Sem upload de arquivo wired (mesmo padrão de `fotos` em portfólio/produtos — P-018): `arquivos`/`imagemCapaUrl` entram como URLs de texto puro. Validado no browser: vitrine e detalhe tratam indisponibilidade da API corretamente, `/compras` e `/conta/prestador` redirecionam pro login sem sessão (middleware funcionando), sem erro de console em nenhuma tela. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam — ver P-071 |
| 2026-08-04 | Merge de `feat/web-catalog` em `main` (fast-forward) — catálogo de plantas (E9-05, 3ª e última rodada de telas do épico E9). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes. **Épico E9 completo de ponta a ponta (backend + frontend)**: notícias, biblioteca, indicadores, custos médios, anúncios, catálogo de plantas com checkout simulado e marca d'água real |
| 2026-08-04 | Moderação de perfis — backend (E10-01, 1ª rodada do épico E10, painel admin — confirmado com o usuário via `AskUserQuestion`: suspender/reativar conta sem histórico dedicado, `apps/admin` espelhando o padrão de sessão de `apps/web`). Novo `User.suspenso`/`suspensoMotivo`/`suspensoEm` + `AdminModule` (`GET/PATCH /admin/users...`, ADMIN-only) — suspender revoga todas as sessões e bloqueia login/refresh, sem revogar um access token já emitido (mesmo trade-off de P-020). Disputas e conteúdo já tinham endpoint ADMIN pronto — não precisou de backend novo pra essas partes. Schemas zod validados isoladamente (`node -e`). `prisma validate`/`generate`, `tsc`/`nest build`, boot-test (só P1001 esperado), `pnpm build`/`lint`/`test` da raiz passam — ver P-072. Próxima rodada: scaffold real do `apps/admin` (Next.js, login, middleware) |
| 2026-08-04 | Merge de `feat/admin-users-backend` em `main` (fast-forward) — moderação de perfis, backend (E10-01, 1ª rodada do épico E10). Revalidado em `main`: `prisma validate`/`generate`, `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: scaffold do `apps/admin` (Next.js, login ADMIN, middleware de sessão) |
| 2026-08-04 | `apps/admin` deixou de ser scaffold vazio — app Next.js real (E10-01, 2ª rodada do épico E10). Login restrito a `ADMIN` (rejeitado no Route Handler antes de emitir cookie, checado de novo no dashboard), cookies de sessão com nomes distintos de `apps/web` (evita colisão em dev, mesmo `localhost`), `middleware.ts` protegendo tudo por padrão (inverso do padrão de `apps/web`). Dashboard é só um shell com 3 cards placeholder — nenhuma tela funcional ainda. `.claude/launch.json` ganhou entrada `admin` (porta 3001). Validado no browser: redirect sem sessão, erro de indisponibilidade tratado corretamente, sem erro de console. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam — ver P-073. Próximas rodadas: telas de usuários, disputas e conteúdo (todo backend já existe) |
| 2026-08-04 | Merge de `feat/admin-app-scaffold` em `main` (fast-forward) — scaffold real do `apps/admin` (E10-01, 2ª rodada do épico E10). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: tela de usuários (busca/detalhe/suspender/reativar) |
| 2026-08-04 | Tela de usuários no `apps/admin` (E10-01, 3ª rodada do épico E10): `/usuarios` (busca/filtro) + `/usuarios/:id` (detalhe + suspender/reativar). Porta do admin trocada de 3001 para 3096 (pedido do usuário) — atualizada em todos os arquivos de config/docs que a citavam. Card "Usuários" no dashboard agora linka de verdade. Validado no browser (redirects de sessão corretos, sem erro de console). `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam — ver P-074. Próxima rodada: disputas ou conteúdo |
| 2026-08-04 | Merge de `feat/admin-users-frontend` em `main` (fast-forward) — tela de usuários no admin + troca de porta 3001→3096 (E10-01, 3ª rodada do épico E10). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: fila de disputas ou CRUD de conteúdo no admin |
| 2026-08-04 | Fila de disputas no `apps/admin` (E10-01, 4ª rodada do épico E10, completa E4-09/E4-10 com UI). `DisputesService.listAbertas()` enriquecido com contexto (obra/etapa/valor/quem abriu, novo tipo `AdminDispute`) — necessário pra mediação de verdade. Nova página `/disputas` + `ResolveForm` + Route Handler `/api/disputas/:id/resolver`. Card "Disputas" linkado no dashboard. `tsc`/`nest build`/boot-test/`next build`/`pnpm build`/`lint`/`test` da raiz passam — ver P-075. Próxima rodada: CRUD de conteúdo (notícias/biblioteca/indicadores/custos médios) |
| 2026-08-04 | Merge de `feat/admin-disputes` em `main` (fast-forward) — fila de disputas com mediação no admin (E10-01, 4ª rodada do épico E10). Revalidado em `main`: `prisma generate`, `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: CRUD de conteúdo (notícias/biblioteca/indicadores/custos médios) no admin |
| 2026-08-04 | CRUD de conteúdo no `apps/admin` (E10-01, 5ª e última rodada do épico E10 — **completa o painel admin**). `/conteudo` (landing) + `/conteudo/artigos` (CRUD completo) + `/conteudo/indicadores` e `/conteudo/custos-medios` (lista + upsert, sem exclusão — backend nunca teve DELETE pra nenhum dos dois). Sem backend novo — tudo já existia desde E9-01/02/03. Card "Conteúdo" linkado no dashboard. Validado no browser: 4 páginas redirecionam pro login sem sessão, sem erro de console. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam — ver P-076 |
| 2026-08-04 | Merge de `feat/admin-content` em `main` (fast-forward) — CRUD de conteúdo no admin (E10-01, 5ª e última rodada do épico E10). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes. **Épico E10-01 completo**: moderação de perfis, fila de disputas e CRUD de conteúdo no `apps/admin` |
| 2026-08-04 | Testes E2E do fluxo crítico + carga do matching (E10-04) — primeira suíte de testes real do monorepo. `apps/web/e2e/critical-flow.spec.ts` (Playwright, híbrido browser+API confirmado com o usuário): cadastro via navegador (única parte com UI real) + resto do fluxo (obra→RFQ→matching→proposta→aceite→milestone→escrow→liberação→extrato) via chamada direta a `services/api`, já que essas telas não existem em `apps/web`. `services/api/test/load/matching-k6.js` mede carga do matching. Gerador de CPF válido testado isoladamente (10k amostras, 0 falhas). **Nenhum dos dois nunca rodou contra a stack real** (sem Docker/k6 neste ambiente) — validado até onde dá: `tsc`, `next build`, `playwright test --list`. `pnpm build`/`lint`/`test` da raiz passam — ver P-077 |
| 2026-08-04 | Merge de `feat/e2e-critical-flow` em `main` (fast-forward) — testes E2E do fluxo crítico + carga do matching (E10-04). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes |
| 2026-08-04 | Cadastro assistido de prestadores no `apps/admin` (E10-02) — `/prestadores-piloto`, formulário único combinando conta+perfil numa chamada orquestrada (`POST /api/prestadores-piloto`: register + profile, usando o token do prestador recém-criado só server-side). Contador "X/200" reaproveita endpoint já existente de moderação. Sem backend novo em `services/api`. Card linkado no dashboard. Validado no browser: redireciona pro login sem sessão, sem erro de console. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam — ver P-078 |
| 2026-08-04 | Merge de `feat/admin-cadastro-assistido` em `main` (fast-forward) — cadastro assistido de prestadores-piloto (E10-02). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes |
| 2026-08-04 | Analytics de funil + KPIs — backend (E10-03, 1ª rodada). `AnalyticsService` (PostHog, opcional/no-op sem API key) instrumentado nos pontos reais do funil (cadastro, RFQ criado, proposta enviada/aceita, depósito em escrow, pagamento liberado). `GET /admin/kpis` calcula os 6 KPIs do PRD direto do Postgres, sem depender do PostHog — determinístico. MRR/churn sempre 0/null hoje (sem billing real, E8). Lógica de contagem validada isolada (`node -e`). Sem UI ainda. `tsc`/`nest build`/boot-test/`pnpm build`/`lint`/`test` da raiz passam — ver P-079. Próxima rodada: dashboard `/kpis` no admin + inicialização do PostHog client-side em `apps/web` |
| 2026-08-04 | Merge de `feat/analytics-kpis-backend` em `main` (fast-forward) — analytics de funil + dashboard de KPIs, backend (E10-03, 1ª rodada). Revalidado em `main`: `prisma generate`, `pnpm build`/`lint`/`test` da raiz — todos verdes. Próxima rodada: dashboard `/kpis` no admin + PostHog client-side em `apps/web` |
| 2026-08-04 | Analytics de funil + dashboard de KPIs — frontend (E10-03, 2ª e última rodada, **completa o épico**). `/kpis` no admin (6 cards com valor/meta/badge). PostHog client-side em `apps/web` (`lib/posthog.ts`, `<PostHogPageview>` no layout, eventos `cadastro_concluido`/`login_concluido`) — tudo no-op sem `NEXT_PUBLIC_POSTHOG_KEY`. Achado de build: `posthog-js` duplicado no chunk de `/cadastro`/`/entrar` em vez de deduplicado com o layout — funciona, não é ideal, revisitar se incomodar. Validado no browser: sem erro de console, `/kpis` redireciona sem sessão. `tsc`/`next build`/`pnpm build`/`lint`/`test` da raiz passam — ver P-080 |
| 2026-08-04 | Merge de `feat/analytics-kpis-frontend` em `main` (fast-forward) — dashboard de KPIs + PostHog client-side (E10-03, 2ª e última rodada). Revalidado em `main`: `pnpm build`/`lint`/`test` da raiz — todos verdes. **Épico E10-03 completo** |

---

## 📌 Como publicar esta branch (P-004)

> ~~Obsoleto (2026-07-28)~~: o branch `sprint-0` nunca chegou a existir — o
> scaffold foi commitado direto em `main`, e toda a pilha desta sessão
> (S0-04 a E3-02) também já foi mesclada em `main`. P-004 permanece aberto
> só como registro histórico do fluxo pretendido originalmente.

```bash
# na sua máquina, dentro do clone de https://github.com/lkohler1979/ConectaObra20
git checkout -b sprint-0
# copie o conteúdo do pacote sprint0 por cima do repositório (preservando .git)
git add -A
git commit -m "chore(sprint-0): scaffold monorepo, CLAUDE.md, PENDENCIAS.md e docs de kickoff"
git push -u origin sprint-0
# abra o PR: sprint-0 → main
```
